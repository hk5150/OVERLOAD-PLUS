# ビルド化(Vite/esbuild)に関する決定事項メモ

対象: iOSアプリ(Capacitor)の起動速度改善のためのビルド化作業。
GitHub Pages配信用の`index.html`本体は対象外(後述)。

## 背景

外部レビューでの指摘:
- `vendor/babel.min.js`が2.87MB(gzip後604KB)あり、起動のたびに`Babel.transform()`で約3,500行のJSXを変換していた
- 実測(デスクトップ、高速CPU): `Babel.transform()`単体で160〜200ms。実機のiPhone(特に旧機種)ではこれより数倍かかる可能性が高い
- `eval()`を使う構成のため、CSPを`unsafe-eval`なしまで厳格化できない
- App Store審査そのものをブロックする要因ではないが(起動速度・CSP・審査時の説明のしやすさの観点で)不利

## 決定事項

### 1. Vite全面移行ではなく、iOSビルド(`www/`)だけをスコープにした

このリポジトリの`index.html`は「ビルド不要で直接編集できる」ことを設計原則にしている(CLAUDE.md参照)。もともとclaude.aiのArtifactとして作られた経緯もあり、GitHub Pages配信用の`index.html`をビルド成果物にしてしまうと、その原則が崩れる。

そのため:
- **GitHub Pages版(`index.html`)は無変更。** 今まで通りランタイムBabelで動く。zero-buildのまま
- **Capacitor(`www/`)側だけ**、`npm run sync-www`実行時に事前ビルドする方式にした

### 2. Babel(`@babel/core`)ではなくesbuildを採用

- 依存が軽い(単一バイナリ、`@babel/core`+プラグイン一式より小さい)
- JSX→`React.createElement`のclassic変換を明示的に指定でき、既存コード(グローバルの`React`/`ReactDOM`を使うUMD構成)とそのまま噛み合う
- 変換速度が速く、`npm run sync-www`の実行コストが問題にならない

### 3. 出力の構成

`scripts/sync-www.js`が以下を行う:
1. `index.html`から`<script type="text/plain" id="appsrc">`のJSXを取り出す
2. esbuildで事前トランスパイル(`jsx: "transform"`, `target: "es2019"` — `??`/`?.`等のES2020構文を下位互換のため変換)
3. `www/app.bundle.js`として書き出す
4. `www/index.html`の起動スクリプトを、ライブラリを順に読み込む`<script src>`列 + `<script src="app.bundle.js">`だけの単純な形に差し替え(Babel読み込み・`Babel.transform()`・`eval()`はすべて無し)
5. `vendor/babel.min.js`(2.87MB)は`www/vendor/`にコピーしない

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `scripts/sync-www.js` | 事前ビルドロジックを追加(esbuildでJSX変換、www/index.htmlの起動スクリプトを書き換え) |
| `package.json` / `package-lock.json` | `esbuild`をdevDependencyに追加 |
| `.claude/launch.json` | `www/`をそのまま配信して検証するための`kurabell-www`エントリを追加(ポート8766) |

`index.html` / `sw.js`(ルート、GitHub Pages配信用)は今回のビルド化そのものでは変更していない。

## 検証内容

- `npm run sync-www`実行 → `www/app.bundle.js`(228KB、`React.createElement`呼び出しに正しく変換済み、生のJSX構文は残っていないことを確認)
- `www/`を`kurabell-www`でそのまま配信し、Babel読み込みなし・コンソールエラーなしで起動することを確認
- 分割選択・種目検索・種目追加・セット入力の一連の操作がソースビルド(ランタイムBabel版)と同じ挙動であることを確認

## 残っている課題

### ビルド化まわり
- 実機(iPhone)での起動時間の体感比較は未実施(ローカルでの計測のみ)
- `www/`をさらに圧縮・minifyするかは未検討(esbuildの`minify`オプションは現状オフ)

### 外部レビューで指摘され、まだ着手していない項目
1. ~~**Service Worker**: `vendor/*`をキャッシュ優先にする、`fetch`にタイムアウトを設け電波が弱い環境でのハングを防ぐ、同一origin以外(Google Fonts等)を捕捉しないようスコープを絞る~~ → 対応済み(`sw.js`, CACHE v63)。vendor/\*はキャッシュ優先+ネットワークからの補充、アプリ本体は`AbortController`で4秒タイムアウト後キャッシュへフォールバック、同一origin以外はfetchハンドラで捕捉しないよう`return`で除外。
2. ~~**データ安全性**: `navigator.storage.persist()`の呼び出し、`importBackup`の事前スナップショット退避+検証強化~~ → 対応済み(CACHE v64)。起動時に`navigator.storage.persist()`を呼ぶuseEffectを追加。`importBackup`はワークアウト配列/日付/種目名/セット配列の最低限の構造検証を先に行い、確認ダイアログを通過した後に現在のデータを`workout-log-v1-pre-import`へスナップショット退避してから復元するように変更。
3. ~~**Reactの整合性**: `EXERCISE_OVERRIDES`(モジュールレベル`let`)が`liveVolume`/`weekly`/`prMap`の`useMemo`依存配列に含まれておらず、可動域係数等を変更しても集計に即反映されない~~ → 対応済み(CACHE v64)。`prMap`/`weekly`/`liveVolume`に加えて、同じく`exVolume`経由でROM上書きを参照している`compareBase`/`sameSplitSessions`の依存配列にも`exerciseOverrides`を追加。
4. ~~**パフォーマンス**: 種目メモ欄が1文字ごとに全データを`persist`(JSON.stringify+localStorage書き込み)している。下書き保存の`useEffect`も同様。デバウンス化が必要~~ → 対応済み(CACHE v64)。種目メモ欄は`ExerciseNoteEditor`コンポーネントを新設し、入力中はローカルstateのみ更新、600ms入力が止まってから`persist`を呼ぶように変更。下書き保存の`useEffect`(today/startAt/restStartAt)も800msデバウンス化(`visibilitychange`/`pagehide`での即時flushは変更なし)。
5. ~~**堅牢性**: React ErrorBoundaryが無く、マウント後の例外で画面が白くなると復旧手段がない~~ → 対応済み(CACHE v64)。`ErrorBoundary`クラスコンポーネントを追加し、`<App />`をラップ。例外発生時は再読み込みを促すフォールバック画面を表示(記録データ自体はlocalStorageに残るため消えない)。
6. ~~**アクセシビリティ**: viewportの`maximum-scale=1`がピンチズームを殺している~~ → 対応済み(CACHE v64)。viewport metaから`maximum-scale=1`を削除。

すべて対応済み。次にやるとすれば「ビルド化まわり」の2件(実機起動時間の実測、`www/`のminify検討)。

---

# 2026-08-16: 改名・iOS SQLite移行・App Store申請準備

このセクションはビルド化(上記)とは別件。同じ「決定事項メモ」として一箇所にまとめておく。

## 決定事項

### 1. アプリ名を「KURABELL Workout Log」に変更

`OVERLOAD+`という名前が既存アプリ(KOSEI MASUDA氏の「筋トレ・ワークアウト・ジム 記録 - Overload+」)と
衝突していたため改名。検討順序: OVERLOAD+ → MAESET → KURABELL+ → KURABELL Workout Log(最終)。

- Bundle ID: `com.hajime5150.kurabellplus`(未申請だったため自由に変更できた)
- App Store掲載名・タイトル類は正式名「KURABELL Workout Log」
- ホーム画面アイコンラベル・アプリ内ヘッダーロゴは「KURABELL」(コンパクト表記。
  `manifest.json`の`name`/`short_name`の使い分けと同じ考え方。文字数制限・見切れ対策)
- リポジトリ名・GitHub Pages URL(`OVERLOAD-PLUS`)は意図的に変更していない
  (審査上必須ではなく、変更するとリンクが切れるリスクの方が大きいため)

### 2. iOS版の永続化をCapacitor Preferences(UserDefaults)からSQLiteへ移行

数年分のトレーニング履歴を1個のJSON文字列としてUserDefaultsへ毎回丸ごと書き直す設計は、
性能・破損・保存失敗のリスクがあったため。採用ライブラリは`@capacitor-community/sqlite@^6.0.2`
(現在のCapacitor 6系と`peerDependencies`が一致する最後の系列)。設計判断・スキーマ・移行手順の
詳細は[DATA_MIGRATION.md](../DATA_MIGRATION.md)を参照。ここでは要点だけ:

- `workouts`/`workout_exercises`/`sets`のみ正規化。`settings`/`custom_exercises`はキー1行=JSON文字列1個
  (形の決まっていない小さな設定値をカラム分割すると、フィールド追加のたびにDDL変更が要るため)
- 旧Preferencesデータからの移行は1回きり・1トランザクション・失敗しても次回起動時に再試行可能
- `workouts`配列への参照が変わっていない保存(プロフィール編集など)はworkouts系テーブルに触れない
  最適化(UserDefaults時代の「小さな変更で履歴全体を書き直す」問題を繰り返さないため)
- Web版は変更なし(引き続きlocalStorage)。`src/domain/storage.js`が`workout-log-v1`キーだけを
  ネイティブ+SQLiteプラグインが使える環境でSQLite経由にすり替える

### 3. Xcodeシミュレータでの実機相当検証で見つけたバグを修正

`CapacitorSQLite.query()`の戻り値(`values`配列)は、**先頭要素だけが`{"ios_columns":[列名,...]}`
という特殊なメタデータ行**という仕様だった。Swiftソースの型だけを見て「先頭行も含めて全部が
`{列名:値}`の行オブジェクト」と誤読して実装し、実機で「settings行のJSON.parseに失敗」という
バグを引き起こした。`capacitorSqliteDriver.js`に`normalizeRows()`を追加して修正。
詳細は[DATA_MIGRATION.md](../DATA_MIGRATION.md)の「実機検証で見つけた不具合」を参照。

同じ検証で、旧バージョンからの更新(Preferencesに旧データがある状態からの起動)・二重移行防止・
前回記録の表示・バックアップ書き出しが実機で正しく動作することも確認済み。

### 4. その他のP0対応

- `support.html`新規作成、Support URLをmailtoから`support.html`のURLに変更
- Privacy Manifest(`PrivacyInfo.xcprivacy`)にSQLite用のFile Timestamp API宣言(`C617.1`)を追加
- YouTube検索機能とprivacy.html/APPSTORE.mdの「外部通信なし」表記の矛盾を解消
  (「主要機能はオフライン、YouTube検索時のみ外部接続」という正確な記述に統一)
- バックアップJSONに`formatVersion`/`appVersion`/`platform`を追加、復元時の検証を強化
  (数値異常・日付・未知のformatVersion拒否)
- 初回起動時に分割設定を強制せず「今すぐ記録する」を選べるように変更。分割未設定での
  初回記録後に「メニューとして保存しますか?」と提案する導線を追加
- アクセシビリティ: 未ラベルのアイコンボタンに`aria-label`追加、狭い画面幅(iPhone SE相当)での
  ボタン折り返し崩れを修正、通知音トグルに`role="switch"`/`aria-checked`を追加

## 変更したファイル(主なもの)

| ファイル | 内容 |
|---|---|
| `index.html` | 改名、SQLite関連ファイルのLIBS追加、初回体験改善、a11y修正 |
| `src/domain/storage.js` | `workout-log-v1`のみSQLite経由に振り分けるルーティングを追加 |
| `src/domain/db/schema.js` | SQLiteスキーマ定義(新規) |
| `src/domain/db/migration.js` | 旧JSON⇔SQLite行の変換ロジック(新規) |
| `src/domain/db/workoutStore.js` | 移行・読み書きのオーケストレーション(新規) |
| `src/domain/db/capacitorSqliteDriver.js` | ネイティブSQLiteプラグインへの薄いラッパー(新規) |
| `src/domain/backupValidation.js` | `validateBackupPayload`・formatVersion検証を追加 |
| `capacitor.config.json` / `ios/App/App/Info.plist` / `ios/App/App.xcodeproj/project.pbxproj` | 改名・SQLiteプラグイン設定 |
| `ios/App/App/PrivacyInfo.xcprivacy` | File Timestamp API宣言を追加 |
| `support.html` / `MONETIZATION.md` / `APP_REVIEW_CHECKLIST.md` / `DATA_MIGRATION.md` | 新規作成 |
| `APPSTORE.md` / `README.md` / `CLAUDE.md` / `IOS_SUBMISSION_GUIDE.md` / `privacy.html` | 改名・内容更新 |
| `tests/db/*.test.js` / `tests/helpers/fakeSqliteDriver.js` | SQLite永続化層のテスト(新規、node:sqliteのフェイクドライバ使用) |

コミット: `c7ca114`(改名+SQLite移行本体)〜`374e930`(移行検証)。

## 検証内容

- `npm test`: 141件パス(スキーマ・移行・setAll/getAll・storage.js経路切り替え・
  実機で踏んだ行データ形状の回帰テストを含む)
- Xcodeシミュレータ(iPhone 17 Pro)で実際にビルド・起動・操作して確認:
  新規インストール、アプリ完全終了後の再起動、旧バージョンからの更新、二重移行防止、
  前回記録のUI表示、バックアップ書き出し

## 残っている課題

- **TestFlight/実機(シミュレータでなく物理iPhone)での最終確認は未実施**
- 機内モード相当のオフライン動作確認はシミュレータでは検証困難だったため未実施
  (Macのネットワークを切ると他の作業に影響するため。主要機能はコード上もともと
  通信を必要としない設計)
- 「すべての履歴を削除」ボタンの実機タップ操作は座標特定が難航し断念
  (`clearAll()`自体はunit testでSQLite/Preferences両方から削除されることを確認済み)
- P1のうち休憩タイマーのiOS通知は対象外(現状維持でよいとの指示)
- P2(可動域係数のオン/オフ、推定1RMの説明改善)は未着手
- StoreKit実装は未着手(`MONETIZATION.md`で案A「買い切り」を推奨、実装はこれから)
- ホーム画面に旧Bundle ID(`overloadplus`系)の古いインストールが別アプリとして残っている
  可能性がある(実機で不要なら手動削除が必要)

---

# 2026-08-18: 開発支援エージェントチームの構築

このセクションもビルド化・SQLite移行(上記)とは別件。

## 背景

多言語化Phase 1〜3の作業中に、実装者本人が同じ文脈で確認したために見逃した不具合が2件あった
(英語文言の前置詞重複、セーフエリアの原因を実測せず断定して誤記録)。どちらもテストでは
捕まらない種類で、別文脈からの検証が要ると判断し、調査・検証専用のサブエージェントを構築した。

## 決定事項

### 1. 実装はメインスレッド1本、サブエージェントは調査と検証に限定

`index.html`に3,776行が集中しており、実装を複数エージェントに分割すると衝突と設計の
不整合を招くため。実装用サブエージェントは作らない。

### 2. 当初案を調査で3点修正

- **`npm test`は既にJSX構文エラーを捕捉していた**(`tests/sync-www.test.js`の`beforeAll`で
  `build()`を実行し、esbuildが`#appsrc`をJSXとしてパースするため)。CLAUDE.mdの罠リスト筆頭
  「JSXの構文エラーはBabelが走るまで表面化しない」への正しい打ち手はエージェントでなくフックだった
- **逆にテストは「起動するか」を一切保証していない**(`vitest.config.mjs`に明記のとおり
  `index.html`自体は実行しない)。ここが実は最大の穴で、当初案の5エージェントはどれも
  埋めていなかった。`boot-check`エージェントを追加して対応
- **CLAUDE.mdの「SQLite未検証」記述が既に腐っていた**(`6b4d1b4`・`374e930`で検証済みだった)。
  エージェント構築前に訂正した

`security-auditor`は認証・認可・シークレットが存在せず空振りしやすいため削除し、
観点は`reviewer`と外部通信テストの強化に吸収した。

### 3. 各エージェント共通の制約

- `www/`と`ios/App/App/public/`は生成物なので調査対象外
  (`app.bundle.js`に同じコードがもう2セット存在し、誤って実装箇所として引用する事故が起きるため)
- 行番号でなく一意なコード片を引用する(複数セッションが並行編集するため行番号は陳腐化する)
- CLAUDE.mdの罠一覧をエージェント定義に複製しない(複製すると更新時にそちらが先に腐る)

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `.claude/agents/explorer.md` | `#appsrc`(3,776行)への横断調査(新規、sonnet指定) |
| `.claude/agents/researcher.md` | 外部技術調査。既定ではファイルに書き出さない(新規) |
| `.claude/agents/reviewer.md` | 実装後の別文脈レビュー。重大/改善提案/好みの3分類(新規) |
| `.claude/agents/tester.md` | 不足テストの追加のみ。既存テストのアサーション変更禁止(新規) |
| `.claude/agents/boot-check.md` | 起動確認(Web版8765とwww版8766の両方を必ず開く)(新規) |
| `.claude/hooks/run-tests.sh` | `index.html`等の編集後に`npm test`を自動実行し、失敗をadvisoryで通知(新規) |
| `.claude/settings.json` | `permissions.deny`で`www/`と`ios/App/App/public/`への書き込みを拒否、`run-tests.sh`をフック登録 |
| `CLAUDE.md` | SQLite未検証の記述を実態に訂正、「検証手段と、その守備範囲」「進め方」セクションを追記 |
| `tests/sync-www.test.js` | 外部ホスト検査を拡張(`src`/`href`属性だけでなく`fetch`/XHR/WebSocket/CSSの`url()`まで) |

## 検証内容

- `npm test` 187件通過
- JSX構文エラーを故意に挿入し、`run-tests.sh`フックが検出・原因特定まで示すことを確認
- `fetch("https://...")`を故意に挿入し、拡張した外部通信検査が検出することを確認
- `www/`配下へのEditが`permissions.deny`で実際にブロックされることを確認
- 5エージェント全ての frontmatter を検証(`name`/`description`必須、`tools`/`model`/`memory`)

コミット: `fe201a8`。

## 残っている課題

- `.claude/agents/`はセッション再起動後でないと読み込まれない。構築したセッション内では
  `reviewer`を実際に呼べておらず、動作確認は未実施
- 次のセッションで5エージェントを実際に走らせて、意図どおりの粒度・判定が返るか確認する必要がある
- `security-auditor`の代替としたテスト強化(外部通信チェック)は1回の実装のみ。
  新しい通信APIが増えたときに検出パターンを追従できるかは未検証

---

# 2026-08-19: ユーザー要望6件の実装

このセクションも上記2件とは別件。ユーザーから投げ込まれた11件の改善要望を調査・検討し、
優先順位「5番(確認のみ)→9番・2番(低コスト)→6番・7番」の指示どおりに実装した。

## 決定事項

### 1. RIR未入力セットの除外(5番)は調査の結果、実装済みと判明

`saveWorkout`/`saveEditWorkout`のフィルタ条件(`reps>0 && (warmup || RIR入力済み)`)が
既に要望どおりに動いていた。実機で3セット入力(RIR入力1件・未入力2件)して保存し、
履歴に1セットのみ残ることを確認。**コード変更なし。**

### 2. 部位名「胸」→「大胸筋」(2番)

解剖学的に不正確な「胸」を正しい部位名に改名。`normMuscle()`に「胸/上部胸筋→大胸筋」の
正規化を追加し、既存記録のmuscle文字列が書き換わらなくても起動時に自動移行されるようにした
(過去の「背中→広背筋」と同じパターン)。これが無いと旧「胸」と新「大胸筋」が週間集計・
分割日マッチで別グループに分離されてしまう。

**SPLIT_PRESETSのDay表示ラベル(`name: "胸"`)とi18nの`DAY_NAMES_EN`/`SPLIT_NAMES_EN`は
意図的に変更していない。** 5分割プリセットの短縮ラベルとして「胸/背中/脚/肩/腕」の
一貫性を保つため(「背中」も同様にMUSCLES配列には存在しない旧称由来の短縮語)。

### 3. ヘッダーの文字サイズ(9番)

「KURABELL Workout Log」が単一テキストノードで同一フォントサイズだったのを`<span>`で分割し、
「Workout Log」部分だけ26px→17pxに縮小。

### 4. インターバルゲージの連続化(6番)

下部の3分割バーが`restElapsed >= m*60`の二値判定で、60秒ごとにしか色が切り替わらない
実装だった。数字のカウントアップは0.5秒間隔で動いているのにゲージは止まって見える問題が
あったため、各60秒セグメント内の経過割合を連続的に幅で表現するよう変更。

### 5. 停滞アラート(7番、新機能)

既存の`exerciseInsight`内のstreak計算(直近から遡って同じトップ重量が何回続いたか)を
そのまま流用し、3回以上で「そろそろ上げどき」のバッジを表示する。新しく1RMベースの
別ロジックを作らなかったのは、既存UIの「{重量}で{n}回連続」表示と自然に統合でき、
進歩性過負荷の観点でも「同じ重量が続いている」ことが最も直接的な指標のため。
自動で重量を決めるのではなく気づきを渡すだけに留めている(アプリの中心思想と整合)。

### 6. 保留にした5件(うち8番はクローズ)

3番(記録画面での器具設定)・4番(種目設定を部位ごとに分ける)・8番(マナーモード時バイブ)・
10番(起動タイムラグ)・11番(バックグラウンド音楽の一時停止)は、設計判断や技術的制約が
絡むため未着手だった。調査結果の要点:
- 3番: `today`の種目オブジェクトに器具(`eq`)を保持するフィールド自体が無く、データ構造の拡張が必要
- **8番: クローズ(対応しない)。** `navigator.vibrate`はiOS Safari/WKWebViewで基本的に非対応。
  マナーモード自体もWeb標準からは検出不可能なため「マナーモード時だけバイブ」は技術的に不可能。
  Hapticsプラグインを新規導入すれば振動自体は可能だが、「マナーモード時のみ」という条件分岐が
  実現できないため要望の核心を満たせない。ユーザー判断でクローズ(2026-08-19)
- 10番: 起動ロゴは既に`#boot`で即時表示済み。タイムラグの真因は`vendor/babel.min.js`(2.87MB)の
  ダウンロード・パースと実行時JSXトランスパイル。根本解決にはWeb版のビルド化が必要で、
  「ビルド不要」の設計原則と衝突する
- 11番: `new AudioContext()`の生成がiOSで他アプリの音楽を一時停止させる既知の挙動につながっている
  可能性が高いが、Web標準の範囲での確実な回避策は無い

3番・4番は2026-08-19に実装着手(次セクション参照)。

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `index.html` | `MUSCLES`/`EXERCISE_DB`(14種目)/`SPLIT_PRESETS`の胸→大胸筋、`normMuscle`に正規化追加、ヘッダーのspan分割、インターバルゲージの連続化、停滞アラート追加 |
| `src/domain/i18n.js` | `MUSCLE_NAMES_EN`のキーを大胸筋に変更、`log.plateauAlert`キー追加 |
| `tests/i18n.test.js` | テストデータの「胸」を「大胸筋」に更新 |
| `sw.js` | `CACHE`をv87→v88→v89に2段階で更新 |
| `CLAUDE.md` | 実装中の検証でもSWキャッシュが古いコードを配り続ける罠を追記 |

コミット: `2ad395f`(5・9・2番)、`cf08d85`(6・7番)。

## 検証内容

- `npm test` 187件通過(テストデータ更新を含む)
- 旧データ(`muscle: "胸"`)を直接投入し、起動時の`normMuscle`正規化で「大胸筋」に
  自動移行されることを実機確認
- インターバルゲージの連続性を実測(`0:41`経過時点で`68.3333%`=41/60と計算どおりの値)
- 停滞アラートの表示境界を確認(3回連続で表示、2回連続では非表示)

### 副産物: 開発中に踏んだService Workerキャッシュの罠

6番・7番の検証中、Web版(port 8765)でコードを変更してもブラウザに反映されない状態に
30分ほどはまった。原因は「バージョン番号を上げる前の実装中の検証」で、一度SWがインストール
されると`cache.addAll()`がブラウザのHTTPディスクキャッシュ越しに古いファイルを掴み、
`unregister()`/`caches.delete()`をしても再インストール時に同じ古い版を拾い直すことがある。
最終的にSWを使わないwww版(port 8766、別origin)に切り替えて確実に検証した。
詳細は`CLAUDE.md`の「踏み抜きやすい罠」に追記済み。

## 残っている課題

- 3番・4番・8番・10番・11番(上記「保留にした5件」)は未着手のまま
- 8番はHapticsプラグイン導入が前提になるが、`package.json`にまだ追加していない
- 10番の根本対応(Web版のビルド化)は「ビルド不要」という設計原則との兼ね合いをユーザーと
  改めて合意してから着手する必要がある

---

# 2026-08-20: 保留5件のうち3番・4番に着手、8番はクローズ

前セクションで保留にしていた11件の要望のうち、ユーザーの指示で3番(記録画面での器具設定)・
4番(種目設定を部位ごとに分ける)を実装し、8番(マナーモード時バイブ)はクローズした。

## 決定事項

### 1. 8番はクローズ

技術的に実現不可能と判明済み(`navigator.vibrate`がWKWebView非対応、マナーモード自体も
Web標準から検出不可能)だったため、ユーザー判断でクローズ。保留5件の記述を更新済み。

### 2. 3番は「器具を選べる機能」ではなく「カスタム種目追加時に器具が強制的にダンベルになる不具合」の修正だった

要望原文は「記録画面で新しい種目をカスタム追加すると、強制的にダンベルになる」。当初は
「器具ごとに前回実績を別トラックとして記録したい」という新機能かと想定して調査を始めたが、
ユーザーに確認した結果、実態は不具合修正だと判明。前回実績のゴースト比較(このアプリの
中心機能)は器具に関係なく種目名だけで行う「現状維持」で良いことも確認済み。

原因は2つ:
- `customDraft`(カスタム種目追加時の下書き)の`eq`初期値が`"ダンベル"`固定で、ユーザーが
  明示的に選ばない限り常にダンベル扱いになっていた
- 記録画面の「セット間に種目を挿入」パネルには、そもそも部位・器具を選ぶセレクトUI自体が
  存在せず、常に前回選択値(初回はダンベル)を無条件に使っていた

対応: `customDraft`の`eq`初期値を空文字に変更し、下部の通常種目選択パネルのセレクトに
プレースホルダー(`settings.other`キー流用)を追加。挿入パネルにも、下部パネルと同一構成の
muscle/eqセレクトを新規追加した。

### 3. 4番は「使用中の種目を部位ごとに見出し付きで縦に並べる」形にした

現状は「部位チップを1つ選んで絞り込む」方式だったが、「全部位を見出し付きで縦に並べ、
部位チップは該当見出しへジャンプするショートカットにする」方式に変更。母集団(検索なしなら
「使用中の種目」のみ、検索時は`allExercises`全体)は既存のロジックをそのまま維持した。

### 4. reviewerの指摘で2点修正した

`reviewer`エージェントによる別文脈レビューで、実装直後には気づいていなかった2つの不整合が
見つかり、修正した:

- **器具フォールバック値の不整合**: `addExercise`内の`customExercises`登録処理が
  `eq: meta?.eq || customDraft.eq || "その他"`となっており、未選択時に文字列`"その他"`が
  保存されていた。一方、器具を選ぶ全ての`<select>`はプレースホルダーの値を空文字(`""`)で
  統一しており、`<option value="その他">`という選択肢自体が存在しない。保存値`"その他"`と
  select側のoption値`""`が食い違うため、後で設定タブからこの種目を編集しようとすると
  選択状態が壊れる(ブラウザが未一致のvalueを最初のoptionへフォールバック表示するため、
  偶然「その他」と見えることはあるが、Reactの認識としては不一致のまま)。
  `|| "その他"`を`|| ""`に修正し、保存値をプレースホルダーの実値と一致させた。
- **部位チップの母集団ズレ**: 部位チップの生成元が`allExercises`(全94種目+カスタム)のままで、
  実際に見出しとして描画される一覧(既定では「使用中の種目」のみ、母集団がずっと狭い)と
  ズレていた。結果、既定表示では大半のチップがジャンプ先の見出しを持たず無反応になっていた。
  チップの母集団を、リストと同じ`pool`(検索/使用中の絞り込み後の実データ)から算出するように
  修正し、チップとリストの`pool`計算を1つのIIFEに統合した。「未設定」グループへのジャンプ
  チップも同様に追加した。

いずれも`npm test`や目視だけでは気づきにくい種類の不整合(保存データの形とUIの想定形の
すり合わせ漏れ)で、実装者本人による自己レビューを素通りしていた。別文脈レビューの効果が
出た例として記録しておく。

## 検証内容

- `npm test` 187件通過(既存テストへの影響なし、新規i18nキーは追加していない)
- `kurabell-www`(8766、iOS相当の事前ビルド経路)で実機能確認:
  - 記録画面の下部パネル・挿入パネルの両方で、器具未選択のままカスタム種目を追加すると
    `eq: ""`で保存されること(修正前は`"その他"`という孤立した文字列だった)を
    `localStorage`の実データで確認
  - 設定タブで、検索語に応じて実際にヒットする部位のチップだけが表示されること
    (「プレス」で検索すると5部位のみ)を確認
  - `boot-check`エージェントで8765(Web版・ランタイムBabel経路)・8766の両方が
    コンソールエラー無く起動することを確認
- スムーズスクロール(`scrollIntoView({behavior: "smooth"})`)のアニメーション自体は、
  この検証用ブラウザ環境(MCP Browserツール)で`window.scrollTo({behavior: "smooth"})`
  という最も基本的な呼び出しですら効かないことを確認済み(この環境固有の制約)。
  `behavior: "auto"`では正常に動作すること、要素の特定・ジャンプ先の存在判定ロジック自体は
  正しいことは確認できているため、実機のSafari/Chromeでの見た目上の動作(滑らかに
  スクロールするか)は未確認のまま残っている

## 残っている課題

- 3番・4番はスコープ内の対応が完了。ただし4番のスムーズスクロールの見た目は実機未確認
- 調査中に見つかった副次的な事実: 履歴編集画面の「記録し忘れた種目を追加」
  (`editAddExercise`)も、カスタム種目追加時に器具情報が引き継がれない(逆に常に
  `isDb: false`になる)。ただしユーザー報告の症状(「強制的にダンベルになる」)とは
  逆方向の挙動であり、今回のスコープ外として対応していない
- 4番(全部位見出し付き表示)は「使用中の種目」を母集団にする既存ロジックを維持したため、
  一度も記録していない内蔵種目は検索しない限り見出しごと表示されない。これは仕様として
  維持する判断をしたが、`settings.exercisesDesc`の文言(「内蔵種目も含めて設定を
  変更できます」)とニュアンスが完全には一致しない可能性がある
- 保留5件のうち10番・11番は引き続き未着手(前セクション参照)

---

# 2026-08-23: Apple Watch連携の代替として休憩タイマーをローカル通知化、収益化方針を決定

Apple Watch連携をやりたいという相談から始まったセッション。調査の結果、watchOSアプリの
新規開発ではなく、休憩タイマーのローカル通知化という形で実質的な目的を達成する方針にした。
別件で収益化方針(有料/無料)もこのセッションで決定した。

## 決定事項

### 1. Apple Watch連携はwatchOSアプリを作らず、ローカル通知で代替する

調査の結果、以下が判明した:
- **watchOSアプリはWebView技術で作れない**。SwiftUI/WatchKitによる完全な新規ネイティブ実装が
  必須で、`index.html`のコードは1行も再利用できない(Appleのエンジニアがフォーラムで明言)
- 公式の`@capacitor/watch`プラグインは実質メンテナンス停止(最新v0.1.12、peer依存が
  `@capacitor/core: ^5.0.0`でこのアプリの6系とは非互換、最終更新2024-04、公式ドキュメントは404)
- **App Group経由でのSQLite共有は不可能**。`@capacitor-community/sqlite`の実コード
  (`UtilsFile.swift`の`getFolderURL`)が許可リスト外のパスを拒否する実装になっており、
  `iosDatabaseLocation`にApp Groupコンテナを指定する手段が無い
- 一方、**ロック中のiPhoneのローカル通知はApple Watchへ自動転送される**(Apple公式)。
  Watchアプリを書かずに「手首で休憩の終わりを知る」という目的の大部分を達成できる

将来Watchアプリを作るなら、案B(今日のメニュー・前回実績の閲覧、読み取り専用)が
片方向同期で済み費用対効果が最良と判断。案C(Watch単体で記録入力)はドメインロジックが
JSとSwiftに二重化し、個人開発では保守しきれないため非推奨とした。

### 2. 休憩タイマーをローカル通知化(`@capacitor/local-notifications@6.1.3`)

休憩タイマーは`setInterval` + `AudioContext`のみで実装されており、iOSはWebViewが
バックグラウンドに入るとJSタイマーを止めるため、**アプリを閉じると経過も通知音も止まる**
という既存の不具合があった(ジムでスマホを置く実際の使い方で機能していなかった)。
ローカル通知はOS側が発火するためバックグラウンドでも確実に届く。

- **通知と通知音は排他にした**(二重通知の回避)。Capacitor iOSの通知は前面でもバナーを
  出すのが既定のため、ネイティブ+許可済みのときは分刻みの`beep()`を鳴らさず通知に一本化した
- **通知操作を`useRef`のPromiseチェーンで直列化した(実バグ対策)**。`startRest()`は
  重量/回数のonBlurから連続発火し`restStartAt`が短時間に何度も変わるため、直列化しないと
  前の実行の`cancel`が後の実行の`schedule`を追い越して消す経路が実在した(起動時、初回
  レンダーの`cancel`が下書き復元の`schedule`を後から消すケースが最悪)
- 通知許可の要求は「`restStartAt`がnull→非nullに変わったとき」だけ、セッション中1回。
  設定の`soundOn`トグル操作では要求しない(休憩中にトグルすると唐突にダイアログが出るため)
- 新しい設定項目は増やさず、既存の「インターバルの通知音」トグルに相乗りさせた
  (文言は音だけでなく通知も指す内容に更新。「サイレントスイッチがオフだと鳴らない」という
  記述もネイティブでは不正確になるため書き換えた)
- 起動時に前回の予約通知を掃除する専用effectを、タイマーeffectより宣言順で先に配置。
  セット未入力のままインターバルを開始してアプリを終了すると、アプリが存在を知らない
  通知だけが後から鳴る経路があったため

### 3. 収益化方針を決定(別件)

`MONETIZATION.md`の「案A(買い切り)推奨」という下書きを、**案B(無料ダウンロード+
記録10回まで試用+非消耗型IAP)**に変更して決定した。試用条件は暦日ベースではなく
**記録回数ベース**にした。理由: このアプリの中心機能(前回の同じ番手のセットをそのまま
見せる比較表示)は同じ分割日を2周目に回さないと発動せず、5分割だと1〜2週間かかることが
多い。暦日ベースの試用だと、多くのユーザーが中心的な価値を体験する前に試用期間が切れて
しまう。StoreKit実装そのものはこのセッションでは着手せず、Apple Developer Program登録・
Xcode署名・Archive検証といった提出の段取りを先に固めることを優先した。

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `src/domain/restNotifications.js`(新規) | 純粋関数`buildRestNotifications`+Capacitorブリッジ(schedule/cancel/permission) |
| `index.html` | 休憩タイマーの`useEffect`に統合。直列化キュー・許可要求・孤児通知クリーンアップを追加 |
| `src/domain/i18n.js` | 通知文言(`rest.notifyTitle`/`rest.notifyBody`/`settings.notifyDenied`)を新規追加、`settings.sound*`の説明文を更新 |
| `sw.js` / `scripts/sync-www.js` | 3箇所同期(`restNotifications.js`をLIBS/APP_ASSETS/DOMAIN_FILESに追加) |
| `package.json` / `package-lock.json` / `ios/App/Podfile` / `ios/App/Podfile.lock` | `@capacitor/local-notifications`追加 |
| `tests/restNotifications.test.js`(新規) | 純粋関数とフェイクプラグインでのブリッジ関数のテスト(20件) |
| `APP_REVIEW_CHECKLIST.md` | 通知セクション追加。Release構成の検証・GitHub Pagesアクセス確認・iPhone SE/Pro Maxのレイアウト確認など、既存の未検証項目も実機能検証を踏まえてチェック済みに更新 |
| `MONETIZATION.md` | 収益化方針の決定を記録(既存の「案A推奨」は上書き済みとして明記) |

バージョン: v95 → v96。コミット `bf87224`(push済み、`main`ブランチ)。
`ios/App/App.xcodeproj/project.pbxproj`は意図的にコミット対象から除外(下記残課題参照)。

## 検証内容

- `npm test` 207件通過(新規20件、既存187件)
- **iOS Simulatorで実機能検証**: インターバル開始→許可ダイアログ表示→許可→
  バックグラウンド化→システムログ(`log show --predicate 'process == "App" OR eventMessage
  CONTAINS "Notification"'`)で実際にバナー通知(`requestIdentifier: 4203`、3分経過)が
  配信されたことを確認。1・2分の通知(4201/4202)は許可ダイアログの応答待ちの間に「過去」と
  判定され意図通りスケジュールされなかった(バグではなく`buildRestNotifications`の
  「過去の分は積まない」フィルタが正しく機能した結果)
- `reviewer`エージェントによる別文脈レビュー: 重大な指摘なし。改善提案2点を反映済み
  (許可直後にeffectのdeps変化で`schedule`が二重に呼ばれる経路を解消、クロージャが握る
  `restStartAt`の鮮度についてコメントを追加)
- Web版(`kurabell-www`, 8766)で`restNotificationsAvailable()`が`false`を返し、
  従来通り`beep()`のみで動作する(no-op)ことを確認

## 残っている課題

- `ios/App/App.xcodeproj/project.pbxproj`に、Xcodeでのビルド確認時に自動追加された
  `DEVELOPMENT_TEAM`(Personal Team ID)が残っており、意図的にコミット対象から除外している。
  有料メンバーシップ承認後、正式なTeamに切り替えるタイミングで上書きされる見込み
- **通知音が実際に鳴るかは実機/TestFlightでの確認が必要**(シミュレータでは未確認)。
  iOS側で`sound`未指定時にデフォルト音になるか無音かはプラグイン実装依存
- アプリを完全終了(kill)した場合のスケジュール済み通知の挙動、集中モード/通知要約による
  配信遅延は制御外(`interruptionLevel`はこのプラグインで指定できない)
- Apple Developer Programの有料メンバーシップ承認待ち。Archiveビルドは未実施
  (Personal Teamでは実機なしのプロビジョニングプロファイル生成ができないため)
- 次の話題として「記録画面上部のボリューム周りの見え方整理」が挙がっていた(入力中断、詳細未確認)

---

# 2026-08-23(続き): 記録画面上部の固定バーの見え方整理

前セクション末尾で「次の話題」として挙がっていた「記録画面上部のボリューム周りの見え方整理」。
実データを描画して日英・インターバル中・全タブで計測し、7点の問題を確定させてから着手した。

## 見つかった問題(すべて実測で確認)

| 分類 | 内容 |
|---|---|
| 内容 | 「トータル 3,041」は実際には`compareBase.avg`(直近3回の**平均**)。`t("chart.total")`=「トータル」を流用した誤ラベル(3回の合計は9,123) |
| 内容 | しきい値が100%緑/90%黄/未満赤。ボリュームは0から積み上がるので終盤まで赤のまま。自己ベスト更新の「YOU WIN!」と赤い`63%`が同じ画面に並ぶ |
| 内容 | 比較基準の説明が2行に散っていた(ラベル行の「(vs 直近3回の胸)」と3行目の「トータル 3,041」が同じものを指す) |
| レイアウト | 最初のRIRを入力した瞬間にバーが16px伸びる(実測 69px→85px)。stickyバーなので下の入力欄が丸ごとずれる |
| レイアウト | 上部バーが全タブに出ていた(設定・履歴・分割でも縦85pxを専有) |
| レイアウト | ラベルは11pxで、分割日名を自由に付けられるため長い名前で切れる(使える幅221px、`Chest & Triceps`だと233px必要) |
| 死んだコード | `diff`(未使用の局所変数)、`compareBase.label` / `.volume` / `avgCount`(どこからも読まれない)、`showBar`(常に`true`の定数) |

## 決定事項

### 1. 比較%を細いゲージに置き換えた(%と平均値の文字は消す)

ユーザーと4案(色を中立に / 残量表示 / 細いゲージ / 現状維持)から選択して決定。

**ゲージ全幅を基準の125%とし、80%の位置(=基準ちょうど)に切り欠きを置く。** 全幅を基準の
100%にすると目盛りが右端に来て「超えた」ことを表現できないため。塗りは基準未満が`C.blue`、
到達で`C.green`。既存のインターバル用3分割ゲージ(高さ5px・`borderRadius: 999`・
`transition: width .3s linear`)と同じ見た目の語彙に揃えた。

**正確な基準値はタップ1回で見られる。** 中央カラム全体が既に「同一分割の履歴一覧」モーダルを
開くタップ領域なので、数字を消しても到達不能にはならない。

### 2. 文字で持っていた比較情報は`aria-valuetext`に移した

ゲージは純粋に視覚的なので、そのままだとスクリーンリーダーから比較情報が消える(文字表示を
やめたことによるa11yの後退)。`role="progressbar"` + `aria-valuetext`(`log.volumeRatio`)で
「63%(基準 3,064kg)」と読ませる。`aria-valuenow`は`valuemin`/`valuemax`の範囲(0〜100)に
両側クランプし、範囲外の実値は`aria-valuetext`側が持つ。

### 3. 固定バーは記録タブ限定。ただしインターバル計測中だけは例外

設定・履歴・分割の画面で経過時間とボリュームは判断材料にならない。ただし**計測中だけは
全タブに残す**(`barVisible = view === "log" || restStartAt != null`)。上部バーの
「インターバル開始」ボタン経由では`restFull`が開いていないため、この例外が無いと
「休憩中に履歴で前回の数字を見る」という実際の動線で残り時間も見えず止める手段も無くなる。
これは`reviewer`の指摘で追加した(当初は`view === "log"`のみだった)。

### 4. バーが無いタブにはノッチ帯の目隠しを置いた

`<header>`の`paddingTop`を`calc(16px + env(safe-area-inset-top))`にするだけでは**初期表示
しか守れない**。headerはstaticなのでスクロールすると本文がステータスバーの時刻・電池表示の
裏を流れる(以前はstickyバーが`top: 0`に貼り付いて常に塞いでいた)。`position: fixed`で
`height: env(safe-area-inset-top)`の目隠しを`zIndex: 30`に置いた。`env()`が0のブラウザでは
高さ0になり何も起きないため、**Web版では無症状でiOSでだけ出る**類の問題だった。
これも`reviewer`の指摘。

### 5. ゲージ幅は両側クランプが必須だった

`Math.min(ratio / 1.25, 1)`だけでは`ratio`が負のとき`width: "-32%"`という不正なCSS値になり、
ブラウザが宣言ごと無視して幅auto=トラック全幅になる(**異常値のときだけゲージが満杯に見える**)。
`NumInput`は`type="text"`なのでWeb版ではキーボードから負の重量を打てる。実際に`-500`を
入力して`liveVolume: -3,340kg`を再現し、修正後に塗り幅0pxになることを確認した。
既存のインターバルゲージ(`Math.max(0, Math.min(1, ...))`)からの逸脱でもあった。`reviewer`の指摘。

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `index.html` | 上部バーの中央カラムをゲージ化、`barVisible`の導入、ノッチ目隠しの追加、headerのsafe-area、`compareBase`の整理とコメント修正 |
| `src/domain/i18n.js` | `log.volumeRatio`を追加(読み上げ用)、参照の消えた`compare.prevDay`を削除 |
| `sw.js` | `CACHE`をv96→v97 |

バージョン: v96 → v97。コミット `ec8588f`。

## 検証内容

- `npm test` 207件通過(新規テストなし。変更はすべて表示層で`src/domain/`の純粋関数に及ばない)
- 8766(iOS相当・事前ビルド経路)で実測:
  - 目盛り79.8%・塗り50.5%(=1925/3041/1.25)と計算どおり
  - 100%超で`C.green`かつ塗り100%にクランプ
  - **バー高さは83pxで不変**(基準の有無・ゲージの有無いずれでも。以前は69↔85で16px動いた)
  - `aria-valuetext`が日英とも正しく出る(`162% of baseline (1,190kg)` / `63%(基準 3,064kg)`)
  - 負の重量`-500`を実際に入力し、塗り幅0pxを確認
  - インターバル計測中に履歴タブへ移動してもバーが残り、終了後は消える
  - ノッチ目隠しに47px(iPhone 17 Pro相当)を注入し、スクロールした本文が実際に隠れることを確認
- 8765(Web版・ランタイムBabel経路)でv97起動、記録0件の初期状態でもバー高さ83pxで同一
- `boot-check`エージェントで両経路×4タブの巡回を確認
- `reviewer`エージェントの別文脈レビュー: 重大な指摘なし。改善提案3件(上記3・4・5)をすべて反映

## 残っている課題

- **ノッチ目隠しの実機確認は未実施。** ブラウザでは`env(safe-area-inset-top)`が0で高さ0に
  なるため、47pxを注入して機構が正しいことまでしか確認できていない。シミュレータで
  履歴・設定タブをスクロールして目視するのが確実
- ゲージが125%以上で満杯に張り付くため、150%と300%が見た目で区別できない。色(緑)と
  `aria-valuetext`の実値では区別できるが、視覚的には頭打ちになる。実用上の問題が出るかは未確認
- インターバル中の3列目「タップで拡大」は英語で74px枠に74pxちょうど収まっており余白がゼロ。
  詰めるとラベル側(長いカスタム分割日名で切れる)の余裕を削るため、今回は変更していない

---

# 2026-08-23(続きの続き): 基準ラベルの位置と比較表現の変更

前セクションのゲージ化(v97)を実際の画面で見たユーザーから、2点の修正依頼があった:
「(vs 直近3回の胸)」をバーの下に移動、比率(%)ではなく「あと〇〇kg」の残量表現にする。

## 対応

- ラベル行(「ボリューム」)から`(vs …)`を削除
- ゲージの下に新しい行を追加。`compareBase.avgLabel`(「直近3回の胸」等、既存のcompare.recentDay/
  compare.recentの出力をそのまま流用)+ 残量/超過を組み合わせた文言:
  - 未達: `{avgLabel}の平均まであと{差分}kg`
  - 到達・超過: `{avgLabel}の平均を{差分}kg上回った`
- 新規行も高さを常に確保(`height: 13`固定)。ゲージと同じ理由(条件表示だとRIR入力の瞬間に
  バーが伸びる)

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `index.html` | ラベル行から`(vs…)`を削除、ゲージ下に残量テキスト行を追加 |
| `src/domain/i18n.js` | `log.volumeToGo` / `log.volumeOver` を追加 |
| `sw.js` | `CACHE`をv97→v98 |

バージョン: v97 → v98。コミット `848aaf2`。

## 検証内容

- `npm test` 207件通過
- 8766で実測: 未達時「直近3回の胸の平均まであと1,139kg」、基準を下げて超過を再現し
  「直近3回の胸の平均を735kg上回った」。英語表示も確認(`735kg over Last 3 Chest sessions avg`)
- バー高さは99pxで、未達/超過/記録前のいずれでも不変
- 8765(Web版)でもv98起動・バー99px・コンソールエラー無しを確認

---

# 2026-08-23(4回目): 種目追加パネルの部位フィルタで、最近使った種目を上部に

記録タブの種目追加パネル(下部の通常パネル・「セット間に種目を挿入」パネルの両方が
共通の`pickerList`を参照)で、部位チップ(大胸筋・広背筋等)を選んで絞り込むと、
それまでは`allExercises`の元の順序(EXERCISE_DB定義順)のまま表示されていた。
部位未選択の既定表示は`recentNames`ベースで最近使った順にソートしていたのに、
部位で絞り込んだ途端にその順序が失われる非対称があった。

## 対応

`pickerMuscle`で絞り込む分岐にも、`recentNames`を使ったソートを追加。その部位の中で
最近使った種目が上位に来る(バッジ「最近」も既存のまま出る)。それ以外の種目は
DB定義順を維持(`Array.prototype.sort`の安定性に依存)。

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `index.html` | `pickerList`の`pickerMuscle`分岐にソートを追加 |
| `sw.js` | `CACHE`をv98→v99 |

バージョン: v98 → v99。コミット `073471d`。

## 検証内容

- `npm test` 207件通過(この変更は表示層のみで`src/domain/`には及ばない)
- 8765で実データを投入して実測: 「大胸筋」チップを選択すると、`recentNames`にある
  「ダンベルフライ」が「最近」バッジ付きでリスト先頭に出ることを確認
- 8766でも起動・コンソールエラー無しを確認(ロジックは`#appsrc`内の表示層のみで
  ビルド有無による分岐は無い)

---

# 2026-08-23(5回目): 種目ごとの「AI相談」、履歴カレンダーの常時表示

記録タブの各種目カードから、その種目の相談用プロンプトを持ってAIチャットへ飛べるようにした。
あわせて履歴タブのリスト/カレンダー切替を廃止した。

## 決定事項

### 1. プロンプトはクリップボード経由で渡す。URLのクエリには載せない

**これが今回の中心的な判断。** 「AIサイトを開いてプロンプトを直接表示」も検討したが採らなかった。
理由は3つで、重い順に:

1. **プライバシー申告が崩れる。** `privacy.html` の「これらのデータは本アプリの外部に送信される
   ことはなく」と `APPSTORE.md` の Data Not Collected の根拠説明は、記録を `?q=` に載せて
   chatgpt.com を開いた時点で成り立たなくなる。クリップボードは端末内で完結するので、
   「アプリが送信した」のではなく「ユーザーが貼り付けた」と正しく区別できる
2. **対応が一部サービスに限られる。** Claude は2025-10に `?q=` を削除(再要望も closed as
   not planned)、Copilot は無効化済み、Gemini は元から非対応。ChatGPT は動くが、
   2025-07-11 の修正でクロスサイト遷移では自動送信されず事前入力止まりになった。
   「デフォルトのAIを設定する」機能と、選んだサービスによって挙動が変わる仕様は相性が悪い
3. **公式仕様ではないので黙って壊れる。** 公式にドキュメント化されているのは Claude Code の
   `claude://code/new?q=` だけで、他は全て有志が見つけた非公式。しかも削られる理由が利便性では
   なく**プロンプトインジェクション対策**なので、復活は期待しにくい

なお **URLの長さは制約にならなかった**。当初「日本語はencodeURIComponentで1文字9文字になるので
210文字が上限」と見積もったが、これは全部が日本語の場合の最悪値。実際のプロンプト(種目1つ・
履歴3回分、189字)はエンコード後802字で、安全圏の2,048字に余裕で収まる。重量・回数・日付など
数字が大半を占めるため。**却下の理由は長さではなく上記3点。**

`window.open` による外部遷移自体は既にYouTube検索で前例があり、`tests/sync-www.test.js` の
外部通信検査も「window.openは対象外」とコメントで明示している。

### 2. YouTubeボタンを種目名の行へ移した

種目カードのボタン行は、375px幅で**追加前から既に溢れていた**(行313pxに対しボタン+gapが315px)。
そこへ「AI相談」を足すと更に窮屈になる。YouTube(67px)を種目名の行の右端へ移すことで、
ボタンを1つ増やしながら**行の高さが64px→32pxに減った**(2行→1行)。

副次的に、赤いYouTubeが操作ボタン群から離れて視線を引きすぎなくなった。YouTubeは種目に紐づく
参照リンクなので、種目名の隣にある方が意味的にも自然。

### 3. プロンプトの内容

「次にどの重量・回数で組むべきか」を判断するのに要る材料を、数値の羅列ではなく前提込みで渡す:
種目の設定(部位・器具・片手/体重込み)・体重・目標レップ数・重量の刻み・自己ベスト・
直近5回の全セット・連続回数・今日ここまで・種目メモ。

**RIRの注記(`share.rirNote`)は必須。** これが無いとAIが「余力2」を「2回しかできない」等と
誤読して逆方向の助言をする。同じ理由で、ダンベルの「片手」表記と、自己ベストが実効重量
(両手合計・体重込み)である但し書きも入れている。

### 4. 履歴のカレンダーを常時表示にした

リスト/カレンダーの切替チップを廃止。どちらか一方しか見られない作りで、切替の存在自体が
余計だった。日付タップでその日の記録に絞り込む挙動はそのまま残している。

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `index.html` | `AI_SERVICES` 定義、`exerciseToText`(種目単位のプロンプト生成)、`consultAi` / `copyToClipboard`、種目カードのAI相談ボタン、YouTubeの移動、設定タブのAI選択、カレンダー常時表示、`histMode` 削除 |
| `src/domain/i18n.js` | `share.ex*` / `log.consultAi*` / `settings.ai*` を追加、参照の消えた `history.list` / `history.calendar` を削除 |
| `privacy.html` / `APPSTORE.md` | 外部遷移先が2つになったことを明記(記録を送信しない点は変わらないので申告方針は維持) |
| `sw.js` | `CACHE` をv99→v100 |

バージョン: v99 → v100。コミット `1dc87ad`。

## reviewerの指摘で見つかった実バグ

すべて `npm test` が緑のまま素通りする種類だった。

1. **lb表示のユーザーで「今日ここまで」が二重換算されていた(重大)。**
   `today` のセットの `weight` は**表示単位の文字列**で、他の参照は全て `fromU()` を通している
   (`liveVolume`・`liveBest1RM`・保存時)。プロンプト生成だけが `wU()`(kg→表示単位)を掛けており、
   lbユーザーでは今日の分だけ二重に換算されていた。同じプロンプト内で過去(kg由来・正しい)と
   今日で桁が食い違うため、AIの助言が丸ごと逆方向になる。**kg表示では `wU` が実質恒等なので無症状。**
   `setLine(s, isKg)` に分けて修正し、lb表示で実測確認した(過去 80kg→176.5lb、今日 85→85lb)
2. **コピー失敗を検知できていなかった。** `document.execCommand` は失敗時に例外ではなく `false` を
   返すため、握り潰していた。あわせて、常に textarea を挿入して `select()` する形をやめた
   (クリップボードAPIが使える通常経路では通らないようにした)。重量入力中に押すと
   フォーカスを奪ってiOSのキーボードが閉じるため
3. **`DEFAULT_PROFILE` に `aiService` が無かった。** `defaultProfile()` には足したが、初期stateと
   「全データ削除」に使う定数側を忘れていた。初回起動と削除直後だけ選択状態が消える
4. **自己ベストの但し書きが無かった。** `prMap` の値は実効重量(ダンベルは両手合計)で、
   片手表記のセット行と基準が違う。「自己ベスト44kgに対し今日22kgは大幅減」と誤読される
5. コピー後のラベルが伸びてボタン行が折り返し、下のUIがずれる(英語+375pxで顕著)

## 自分で見つけて修正した分

- 推定1RMが `39.199999999999996kg` と出ていた(画面側は `toFixed(1)` で丸めていた)
- **「今日ここまで」に未実施セットが入っていた。** 種目を追加すると前回のセットが重量・回数ごと
  自動入力されるため、`reps > 0` だけで絞ると「まだやっていないセット」を実施済みとしてAIに渡す。
  CLAUDE.mdの「RIRが入って初めて実施済み」に沿って `rir` の有無で判定するよう修正。
  実際にダンベルフライで、前回と同じ `14kg×12 / 14kg×10` が「今日ここまで」に出ているのを確認した
- ゲージ化のときと同様、文字表示をやめた分の情報が読み上げから消えないよう配慮している

## 検証内容

- `npm test` 207件通過
- `boot-check` で 8765(ランタイムBabel)・8766(事前ビルド)の両経路 × 4タブを確認
- 生成プロンプトを **日英 × kg/lb × バーベル/ダンベル** の組み合わせで実測確認
- 英語+375pxで、コピー後にボタン行の高さが変わらないことを確認(59px固定)
- カレンダー常時表示と、日付タップでの絞り込み(全3件→1件)を確認

### 検証時の罠(CLAUDE.mdの記述の補足)

`boot-check` の調査で、**CLAUDE.mdに書いてあるキャッシュの罠が Service Worker 経路だけでなく、
SWが1つも登録されていない素のブラウザでも起きる**ことが分かった。`src/domain/i18n.js` を
既定キャッシュでfetchすると41,906バイト(旧)、`{cache:'reload'}` だと43,652バイト(新)という
状態になり、画面には `log.consultAi` という生キーがそのまま出ていた。
**クエリ付きURLでのリロードだけでは足りず、`<script src>` の実URLを再検証する必要がある。**

## 残っている課題

- `window.open` がポップアップブロッカーに弾かれたときの経路が無い(戻り値を見ていない)。
  クリップボードには入っているので致命的ではないが、ユーザーには失敗に見える
- 各AIサービスのチャット画面が、実機のiOSでUniversal Link経由ネイティブアプリに飛ぶかは未確認
  (Capacitorは `UIApplication.shared.open()` に委ねるので飛ぶ見込み。実機が要る)
- `exerciseToText` は `#appsrc` 内でstate依存のためテストが無い。1のような単位バグは
  `src/domain/` の純粋関数にしていれば1行のテストで捕まる種類だった

---

# 2026-08-23(6回目): 「今日のメニュー」は前回の実際の記録を優先

## 決定事項

記録タブの「今日のメニュー」(記録開始前に出る提案)の優先順位を変更した。

- 変更前: ①分割タブの登録種目リスト(手動編集しない限り不変) > ②前回の同じセッションの
  実際の記録 > ③過去全履歴から部位一致で拾う
- 変更後: ②前回の同じセッションの実際の記録 > ①分割タブの登録種目リスト > ③部位一致の履歴

分割に種目を登録していても、その種目をもうやらなくなった場合、登録リストは手動編集しない
限り不変なので「今日のメニュー」に出続けていた。「前と同じ種目を引き継ぐ、前回やってない
種目は削除」という要望どおり、前回実際にやった記録があるなら分割の登録内容に関わらず
そちらを優先するようにした。**分割タブの登録リスト自体は削除・変更していない**
(前回記録が無いDayの叩き台として、フォールバックにそのまま残る)。

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `index.html` | `lastSameSession` を新設し `autoMenu` の優先順位を変更、UI文言(`menu.carryover`)の分岐を対応させた |
| `src/domain/i18n.js` | `menu.fromHistory` を追加(部位一致で寄せ集めた場合専用の文言) |
| `sw.js` | `CACHE` をv100→v101 |

バージョン: v100 → v101。コミット `dd5c748`。

## reviewerの指摘で調整した点

重大な指摘は無かった。改善提案2点を反映:

- `autoMenu` の依存配列から `split` を外した。`todayDay`(=`split.days[cursor]`)経由の
  間接参照のみで、`autoMenu` 本体は `split` を直接参照していなかった
- UI文言の3分岐目(前回の同名セッションも分割の登録種目も無く、部位一致で複数の過去記録を
  寄せ集めた場合)が、以前から「前回のセッション」という2値分岐のfromLastに丸められていて
  不正確だった(特定の1回ではなく複数の寄せ集めのため)。`menu.fromHistory`を新設して
  3分岐と1対1に対応させた。最初「過去の記録」としたが、テンプレート`「{src}の記録を
  そのまま引き継いでいます」`と組み合わせると「過去の記録の記録」と重複したため「過去」に修正

## 検証内容

- `npm test` 207件通過
- 実データで3パターンを確認:
  - 分割に3種目登録・前回は2種目だけ実施 → 提案は前回の2種目のみ、分割の登録リストは
    3種目のまま保持される
  - 前回記録が無い新規Day → 分割の登録リストへ正しくフォールバック、文言も「マイ分割に
    登録した種目」で一致
  - 分割登録も前回記録も無く、部位一致の履歴から拾うケース → 文言が「過去の記録をそのまま
    引き継いでいます」と自然になることを確認(修正前は「過去の記録の記録を〜」と重複していた)
- `boot-check`で8765/8766の両経路、記録0件・split=nullの状態でもクラッシュしないことを確認

---

# 2026-08-23(7回目): 可動域係数・体重係数の根拠づけ

ユーザーからの問題提起:「最初に登録されている全種目の可動域係数と自重係数をAIで計算する
ことは可能か。ここが根拠のない弱いところだと思っている」。実際、romは該当8種目が**一律0.5**で、
測定でも計算でもない当て推量だった。

## 決定事項

### 1. 係数そのものではなく、元の物理量をAIに推定させる

最初は「AIに係数を生成させるべきではない」と反対した。理由は、正直な当て推量を
「根拠があるように見える数字」に置き換えるだけになるから。特にROM係数は**参照先が存在しない**
(トレーニング科学の volume load は意図的にROM補正をしないので、探しても出典が無い)。

ただしユーザーの「後でユーザーが変更するので、仮でも実態に近い数字を入れておきたい」という
前提で見方が変わった。**基準が「出典のある真実」から「ユーザーが直す前の出発点」に下がる**なら
達成可能。そこで聞き方を変えた:

```
rom = clamp(荷重が動く距離cm ÷ 40cm, 0.2, 1.0)   ← 式はこちらが持つ
```

AIには**距離(cm)だけ**を推定させる。距離は実在の量でメジャーで検証でき、後から
「なぜ0.3か」に答えられる。体重係数も同様に「係数は?」ではなく「作動筋が支える体重の割合を
**可動域の中間位で**」と聞いた(位置を指定しないとプッシュアップは上端65%・下端75%で揺れる)。

**当初「日本語はURL上210文字が限界」と同様の見積もり違いをした点**: 「romは参照先が無いから
AIに聞いても無意味」と言ったが、**可動距離という物理量には参照先がある**。却下すべきだったのは
「係数を直接聞くこと」で、「AIを使うこと」ではなかった。

### 2. 適用範囲を絞った

式をそのまま当てると20種目が変わるが、**14種目に絞った**。判断基準は
「短い可動域 × 高い実効負荷でボリュームが不当に膨らむもの」。

見送ったのはベントオーバーロウ/Tバーロウ/バックエクステンション/ヒップアブダクション/
ベンチディップスの0.9群。35cm vs 40cmは推定誤差の範囲で、意味のあるシグナルにならず
ノイズを持ち込むだけ。

**弧長の指示が裏目に出た**点も記録しておく。回転種目に弧長を当てるとフロントレイズ95cm・
サイドレイズ85cmとベンチプレス(40cm)より長く出る。上限1.0で頭打ちなので実害は無いが、
「サイドレイズはベンチプレスより仕事量が多い」は成立しない。**この式は「短い種目を見つける」
用途にのみ使えると理解しておくこと。**

### 3. 腹筋を追加(ユーザー指摘)

当初「腹筋のボリューム自体が別問題」として見送ったが、**数字を出すと現状が明らかに過大**だった。
体重70kgでクランチ1セット20回が560kg、ベンチ80kg×10(800kg)の70%。同じ計算で
**プッシュアップ20回が910kgとベンチを上回っており**、「二重減点になる」という私の見送り理由も
間違いだった(ディップス45cm・懸垂55cmは距離が長いので変わらず、短いものだけが下がる
という意図通りの挙動だった)。

## reviewerが見つけた実バグ(3回のレビューで重大5件)

### 新しい既定値が既存ユーザーに永久に届かなかった

`saveWorkout` が `rom`/`bwFactor` を記録に焼き込み、それを**設定値として読み戻していた**ため、
一度でも記録した種目は既定値を更新しても古い値のまま回り続けていた。しかも書き戻すので
自己増殖する。ブラウザで再現を確認(履歴rom:0.5 → 再追加してもROM×0.5)。

**この変更が届く範囲は「その種目を一度も記録したことがない人」だけ**で、目的を達成できて
いなかった。ユーザー自身が既存ユーザーなので、そのままなら何も起きなかった。

### さらにその修正に3つの穴があった

1. **`lastConfig` だけ直しても主経路に効かない。** `buildMenuItem` が `prevEx.rom ?? cfg.rom` と
   記録を直読みしており、`saveWorkout` が必ず数値を書くため `??` が**絶対にフォールバックしない**。
   しかもv101で「今日のメニュー」を前回の実記録優先にしたばかりで、継続ユーザーが毎日通るのは
   こちら。1回目の検証で「修正が効いた」と報告したのは、手動追加経路だけを見ていたため
2. **索引に `dbLookup` を使うとカスタム種目を見ない。** 自重のカスタム種目が `bwFactor: 0` になり、
   実効重量もボリュームも推定1RMも0のまま記録される。目視では気づけない種類
3. **⚙が無編集でも上書きを書くと、潰したバグをUI経由で再生産する。** 欄を触る/チェックを
   往復するだけでマスターと同値の上書きが残り、以後その種目に既定値の更新が届かなくなる

解決を `src/domain/coefficients.js` に切り出し、`lastConfig` と `buildMenuItem` の両方が
同じ関数を通るようにした。設定タブ側にも同じ無編集ガードを入れて非対称を解消。

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `index.html` | 種目マスターの係数24件、解決経路の一本化(`coefficientsOf`)、⚙と設定タブの無編集ガード |
| `src/domain/coefficients.js`(新規) | 係数の解決。記録側の値を受け取る口をあえて持たない |
| `src/domain/i18n.js` | `cfg.romHelp2` / `note.rom` / `cfg.bwFactorHelp2` を実値に合わせて更新 |
| `tests/coefficients.test.js`(新規) | 解決ロジック単体 |
| `tests/exerciseCoefficients.test.js`(新規) | マスターの値の整合 + **#appsrcの配線をテキスト検査** |
| `docs/係数の根拠.md`(新規) | 各種目のcm・計算過程・出典・見送った種目・既知の穴 |
| `CLAUDE.md` / `README.md` | 0.5前提の記述を更新、罠を追記 |
| `sw.js` / `scripts/sync-www.js` | v102、新規ドメインファイルの参照元3箇所 |

バージョン: v101 → v102。コミット `f4497b0`。

## 検証内容

- `npm test` 224件(新規17件)
- **配線テストの実効性を確認**: 3つのバグを故意に元の書き方へ戻し、それぞれ検出されることを確認。
  `npm test` は `index.html` を実行しないため、単体テストだけでは全部緑のまま素通りする
- 実データで検算: クランチ20回が245kg(体重70×bw0.35×20×rom0.5)、シュラッグとの合計545kg
- 「今日のメニュー」経路と手動追加経路の両方で0.3が適用され、値が一致することを確認
- カスタム自重種目の `bw: 0.45` が保たれることを確認(修正前は0になった)
- ユーザー調整(0.55)がマスター更新後も維持されることを確認
- `boot-check` で8765/8766の両経路。新規ファイルが両方で200、`window.resolveCoefficients` が
  関数として存在することも確認

## 残っている課題(docs/係数の根拠.md に詳細)

- `resolveRom` のフォールバックだけ `dbLookup` のままで、カスタム種目を見ない
- 種目マスターから削除した種目は係数を失う(メニューには並ぶのに `bwFactor: 0` になる)
- 記録画面の⚙が「今日の設定」から「グローバル設定」に意味が変わったが、UIの見た目は同じ
- カスタム自重種目で `bw: 0` になると復帰が難しい(「元に戻す」ボタンが出ない)
- 一次情報は無い。94種目の荷重移動距離を列挙した公表資料・査読論文は確認できなかった

---

# 2026-08-23(8回目): ディロード推奨アラートを検討 → 一旦見送り

「RIR0の頻度を自動検知して『そろそろディロード推奨』のようなアラート」という提案を検討した。
実装には至らず、シミュレーションで実用性が無いと判明したため見送った。判断の過程を残す。

## 検討した設計

既存の停滞アラート(同じ重量が3回連続 → 種目カードにバッジ)とは役割を分けた:

- **範囲**: 全体(全部位通算)。ディロードは特定の部位だけ休ませる行動ではなく、
  プログラム全体の強度を落とす判断なので、部位別・種目別では取れる行動と対応しない
- **条件**: 「RIR0が増えている」**かつ**「重量が伸びていない」の両方が揃ったときだけ。
  RIR0だけ多くても重量が伸びていれば順調に追い込めているだけで、重量停滞だけなら
  既存の停滞アラートの領分。この組み合わせだと停滞アラート(「上げどき」)とも衝突しない
- **比較方法**: 絶対しきい値(「40%超えたら」)を避け、**直近 vs その前**の相対比較にする方針。
  「RIR0率40%」も「ボリュームゲージの100%」も参照できる出典が無く当て推量になるため、
  「増えた/増えていない」の二値比較なら当て推量を持ち込まずに済むと考えた

## シミュレーションで判明した問題

週4回・1回12セット、通常RIR0率15%のユーザーが直近2週間だけ35%に悪化するケースを
300〜500試行シミュレーションした(擬似乱数、`node -e` で使い捨てスクリプト)。

**マージン無しの厳密な `>` 比較(直近Nセット vs その前Nセット)は、窓の大きさ(N=12〜40)
を変えても検知率が42〜44%にしかならなかった。** はっきり2倍以上悪化しているケースでも、
半分以上を見逃す。

マージン(倍率)を入れても解決しなかった:

| 設定 | 感度(悪化を検知) | 偽陽性率(平常時の誤検知) |
|---|---|---|
| N=20, ×1.2倍 | 34% | 40% |
| N=96, ×1.2倍 | 99.6% | **31%** |
| セッション8回平均, ×1.3倍 | 99% | **22%** |

**窓を広げれば感度は上がるが、偽陽性率が20〜45%に張り付いたまま下がらない。**
感度を追うほど偽陽性が必ず伴い、実用的な分離点が見つからなかった。

## 原因

マージンの選び方の問題ではなく、**「RIR0か否か」という二値判定を数十件集めて前半・後半で
比率を比べる、というアプローチ自体に統計的な無理がある**。このサンプルサイズでの比率差は、
有意差検定なしで二値判定すると必ず高い誤検知率を伴う。窓を広げてもノイズの性質上ここは
変わらない。

## 決定

**一旦見送り。** 代替案として「傾向表示のみ」(判定をアプリ側でせず、
「直近20セット中のRIR0: 45%(直近4週平均: 20%)」のように数字だけ出して、
増えているかどうかの判断はユーザーに委ねる)を提示したが、それも含めて未着手のまま保留。

「どの段階でディロードにするか」というユーザー自身の最初の懸念が、シミュレーションで
定量的に裏付けられた形になった。

## 再検討する場合の材料

- 実ユーザーのデータが集まれば、擬似乱数ではなく実データで再試算できる。RIR0の出方が
  実際に擬似乱数(独立同分布)とどれだけ違うか(セッション内での相関、種目間の相関)によっては
  結果が変わる可能性がある
- 二値判定をやめて「傾向表示のみ」にする場合、置き場所が決まっていない。上部バーは既に
  経過/ボリューム/インターバルで埋まっており(375px幅で追加の余地なし)、履歴タブの
  ボリューム推移グラフの近くか、記録タブの「今日のセッション」カード下が候補
- 「重量が伸びていない」側の条件(既存の`weekly`のtotalVol/prevTotalVolを流用する想定)は
  未検証。RIR0率と同様のノイズを持つ可能性があり、AND合成した場合の実用性は未確認

## 匿名テレメトリの検討(方針決定: 販売後に再検討)

この流れで「実ユーザーのデータが集まれば、このAND判定を再設計できるのでは」という話も出た。
匿名テレメトリの一般論として以下を確認した:

- 「匿名」の実効性は低い。ユーザーIDを外しても種目構成・重量帯・記録頻度のパターンだけで
  再識別され得る。差分プライバシー等の手当てなしに「匿名」と言い切るのは危うい
- 目的を決めずに広く集め始めると、プライバシーのデータ最小化原則に反するうえ、
  App Storeのプライバシー申告がData Not CollectedからData Collectedへの書き換えになる
  (審査やり直し相当)
- バックエンドが要る。「バックエンドなし」がこのアプリの設計の芯なので、
  受け皿を作ると保存データの管理責任(セキュリティ・削除要求対応)を個人開発者として負う

**方針: データ収集は販売(StoreKit実装・収益化の立ち上げ)後に検討する。** 順番としては
①StoreKit実装 → ②ユーザーベースができてから目的を1つに絞ってオプトインで計測を始める
→ ③その経験を踏まえてディロード判定等を再検討、という流れを想定している。

---

# 2026-08-24: IAP(StoreKit 2)のXcode統合

前々セクション(IAPドメイン層追加, f0a9b36)・前セクション(StoreKit配線, 593ae50)で
コードは揃っていたが、[docs/IAP実装方針.md](IAP実装方針.md)に「Xcodeでの手動作業」として
残っていた統合(Swiftファイルをターゲットに追加、In-App Purchase Capability付与)は
未着手のままだった。このセッションでそのうち2点(ソース追加・フレームワークリンク)を
完了し、統合作業で2つの実バグを発見・修正した。詳細は[docs/IAP実装方針.md](IAP実装方針.md)
に追記済み。ここでは判断の要点だけ記す。

## 決定事項

### 1. Xcode GUIの代わりに`xcodeproj` gemでpbxprojを編集した

[docs/IAP実装方針.md](IAP実装方針.md)は「`.pbxproj`の手編集は壊れやすいため避け、Xcode GUIで
行うこと」としていたが、これは生テキストでの直接編集を指したものと解釈した。CocoaPods/fastlane
自体が内部で使っている`xcodeproj` gem(構造を理解した専用ツール)であれば、生テキスト編集とは
リスクの質が違うと判断し、これで自動化した。`git diff`で差分が意図どおり最小限(ファイル参照
2件+StoreKit.frameworkのリンクのみ)であることを確認し、`xcodebuild`でビルドが通ることまで
確認してから次の作業に進んだ。

### 2. `CAPBridgedPlugin`準拠だけでは自動登録されなかった(実バグ)

Xcode統合後、設定タブに「購入」セクションが出ず、`iapAvailable()`がfalseのままだった。
JS側に`window.Capacitor.Plugins`の中身を出すデバッグ表示を一時的に仕込んで実機能確認したところ、
`Iap`が登録リストから欠落していた(npm経由の3プラグインは出ている)。原因はCapacitorの
プラグイン自動検出が、npm/CocoaPods経由(別ターゲット/フレームワーク)とアプリターゲットへの
直接ソース追加とで登録経路が異なるため。`BridgeViewController.swift`(新規、`CAPBridgeViewController`
サブクラス)の`capacitorDidLoad()`で`bridge?.registerPluginInstance(IapPlugin())`を明示的に呼ぶ
形に変更し、`Main.storyboard`の`customClass`もこれに合わせた。デバッグ表示は確認後に削除し、
`git diff`でindex.htmlに差分が残っていないことを確認した。

`docs/IAP実装方針.md`の「CAPBridgedPluginを実装すれば自動登録される」という記述(593ae50時点)は
誤りだったため訂正した。ドキュメント自体が実装時点の設計意図であり、統合して初めて判明した
制約だったため、これは想定される種類のズレ。

### 3. デプロイターゲットが13.0のままだった(実バグ)

`StoreManager.swift`はStoreKit 2の`Product`型(iOS 15+必須)を使うが、Podfile/pbxprojの
デプロイターゲットはCapacitorの初期値13.0のままで、ビルドがそもそも通らなかった。
iOS 13/14のシェアは2026年時点で実用上ゼロに近く、`@available`でガードして機能を制限するより
デプロイターゲット自体を15.0に上げる方が妥当と判断した(`Podfile`の`platform :ios`とApp
ターゲットのDebug/Release、`pod install`再実行)。

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `ios/App/App.xcodeproj/project.pbxproj` | Iap配下2ファイルをCompile Sourcesに追加、StoreKit.frameworkをリンク、`IPHONEOS_DEPLOYMENT_TARGET`を13.0→15.0 |
| `ios/App/App/BridgeViewController.swift`(新規) | `capacitorDidLoad()`でIapPluginを明示登録 |
| `ios/App/App/Base.lproj/Main.storyboard` | ルートViewControllerの`customClass`を`BridgeViewController`に変更 |
| `ios/App/Podfile` / `Podfile.lock` | `platform :ios`を13.0→15.0 |
| `ios/App/App/Iap/IapPlugin.swift` | 自動登録前提だった冒頭コメントを実態(明示登録)に訂正 |
| `docs/IAP実装方針.md` | Xcode統合の完了状況、2つのハマった点を追記 |

## 検証内容

- `npm test` 250件通過(このセッションでのJS変更なし、確認のため実行)
- `xcodebuild`でDebug/iphonesimulator向けビルドが成功することを確認
- シミュレータ(iPhone 17 Pro Max)で実際にアプリを起動し、設定タブに「購入」セクションが
  表示されること、価格取得エラー(商品ID未登録のため想定どおり)が正しく表示されることを確認
- デバッグ用に一時追加したコードは確認後に削除し、`git diff`で残留がないことを確認

## 残っている課題

- Signing & CapabilitiesでのIn-App Purchase Capability付与、App Store Connextでの商品登録は
  未着手(有料Apple Developer Programの承認待ち。承認後にDEVELOPMENT_TEAMを正式なものへ
  切り替えるタイミングで行う想定)
- XcodeのStoreKit Testing機能(`.storekit`設定ファイル)を使った、実際の購入フロー(成功/
  キャンセル/失敗)のシミュレータ確認は未実施。現状確認できているのは「商品ID未登録時に
  エラー文言が正しく出ること」までで、購入自体の成功パスは商品登録後でないと試せない
- 実機(TestFlight)での動作確認は引き続き未実施

---

# 2026-09-05: 記録画面のUI改善3件(v104)

ユーザーからの要望3件。実装自体は小さいが、レビューで**表示していた1RMが画面ごとに
食い違う既存の不整合**と、**iOS実機でだけ保存ボタンが押せなくなる問題**が出てきたので、
そちらの方が本題になった。

## 要望と対応

### 1. 記録画面の履歴の各日にその日のMAX1RMを表示

種目カードの「履歴」パネル(直近5回)の各日に推定1RMを追加した。履歴タブ側には既に
⚡1RMバッジがあり、**記録タブ側だけ抜けていた**。

### 2. インターバルの時間表示(下部)の数字を大きく

92px → 128px。ただし数字とゲージが横並びのままでは大きくできなかった。「15:06」のような
5文字で数字だけが219pxを占め、ゲージ側に110pxしか残っていなかったため、**縦積みに変更**して
数字とゲージの両方に幅を与えた。

**桁数でサイズを落とす分岐を入れた**(4文字=128px / 5文字=104px / 6文字以上=72px)。
`fmtClock`は分を丸めないので、タイマーを止め忘れたまま翌日開くと(下書きは24時間前まで
復元される)「810:00」「1439:59」になり得る。実測で全ケースが320px幅にも収まることを確認した
(最長の`1439:59`で246px)。

### 3. AI相談ボタンの「ペーストする」が分かりにくい

ボタン行は375px幅で既に一杯(YouTubeを種目名の行へ移してようやく1行に収めた経緯がある)なので、
**ラベルは変えず行の下に注記を出す**形にした(`log.pasteHint`を新設)。AI側の画面から戻ったときに
読めるよう、表示時間を2.5秒→6秒に延ばしている。記録タブと履歴タブの両方に出した。

## reviewerの指摘で見つかった問題

### 重大1: `paddingBottom`の決め打ちが`env(safe-area-inset-bottom)`を2回分取りこぼす

インターバル展開パネルは`bottom: calc(56px + env(...))`に置かれ、パネル自身の`padding-bottom`にも
`env(...)`が入っている。つまり画面下端からパネル上端までは`322px + env() × 2`。**実測値はブラウザ
(env()=0)で採ったものなので、pxの決め打ちだけではiOS実機(env()≈34px)で68px不足する。**

そもそも変更前(250px)から同じ構造の不足を抱えていて、今回の拡大で「Web版では直ったが
iOS実機では保存ボタンが隠れたまま」になるところだった。CLAUDE.mdの「片方だけで動作確認して
満足しない」に該当する。`calc(322px + env(safe-area-inset-bottom) * 2)`に変更した。

### 重大2: `workingSets`は補助あり(assisted)を除かない

`workingSets`の実体は`filter(s => !s.warmup)`で、**assistedは除いていない**。同じ「その日の1RM」を
出すreduceが`#appsrc`内に4箇所コピーされていて、除外条件が3種類に割れていた:

| 経路 | 除外していたもの |
|---|---|
| `prMap`(自己ベスト) | warmup + assisted |
| 履歴タブの⚡バッジ | warmup + assisted |
| `chartData`(重量推移グラフ) | **warmupのみ** |
| 今回追加した履歴パネル(当初) | **warmupのみ** |

補助ありセットを含む日(例: 加重チンニング 本番+10kg×5 → 99.2kg、補助あり 0kg×12 → 実効75kg×12
= 105.0kg)では、**同じ日について画面ごとに違う数字が出る**。検算に使ったベンチプレス等
(補助なし)では一致してしまうので、そのままでは検証をすり抜けていた。

`src/domain/oneRm.js`に`dayBest1RM(sets, isDb, bwFactor, bodyweight)`として集約し、
履歴パネルと`chartData`の両方をこれに通した。**グラフだけが高い値を出していた既存バグも
これで直っている**(要望の範囲外だが、集約の目的そのものなので同時に対応した)。
PRバッジ側(`prIdx`)は「最大だったセットのindex」も必要で単純置換できないため、除外条件が
一致していることを確認して現状維持とした。

### 改善提案として直したもの

- **注記の出入りで下のUIが23px動く問題。** 過去に「コピー後のラベルが伸びてボタン行が折り返し、
  下のUIがずれる」問題を`minWidth: 74`で潰した経緯があるのに、同種の問題を持ち込んでいた。
  AI側から戻った直後にRIR欄をタップしようとした瞬間に6秒が経過して行が飛ぶ、という当たりやすい
  経路だった。高さ17pxを常時確保して中身だけ出し入れする形にした。`role="status"`もDOMに
  常駐させないと読み上げられないため、これで同時に正しくなっている
- **ダンベル・自重種目で1RMに但し書きが無かった。** すぐ下の行にセットが片手/加重の生値で並ぶため、
  「1RM 53.3kg」に対し「20×10」という表示になり誤読を招く。比較カードと同じ
  `share.bothHands`/`share.withBw`を再利用して「(両手計)」「(体重込)」を付けた
- `.playwright-mcp/`(UI検証時のスクショ・ログ)を`.gitignore`に追加

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `index.html` | 履歴パネルへの1RM追加、インターバルパネルの縦積み化・桁数別サイズ、ペースト注記(高さ常時確保)、`paddingBottom`のcalc化、`chartData`を`dayBest1RM`経由に |
| `src/domain/oneRm.js` | `dayBest1RM`を新設(除外条件の集約先) |
| `src/domain/i18n.js` | `log.pasteHint`を追加 |
| `tests/oneRm.test.js` | `dayBest1RM`のテスト21件を追加 |
| `sw.js` | `CACHE`をv103→v104 |
| `.gitignore` | `.playwright-mcp/`を追加 |

バージョン: v103 → v104。

## 検証内容

- `npm test` 271件通過(新規21件)。除外条件を`warmup`のみに戻すミューテーションで、
  追加テストが実際に落ちることを確認済み
- 8765(ランタイムBabel経路)・8766(事前ビルド経路)の両方でv104が読まれることを確認。
  8765では例のSWキャッシュの罠を踏んだので、`unregister()`+`caches.delete()`+
  `{cache:'reload'}`での再検証まで行った
- **補助ありセットを含む自重種目(懸垂)の実データで、履歴パネル・比較カード・履歴タブの
  ⚡バッジ・PR一覧の4箇所すべてが99.2kgで一致することを確認**(修正前は履歴パネルだけ
  105.0kgになる)。ダンベル(53.3kg・両手計)・自重(99.2kg・体重込)の但し書きも実測
- タイマーの桁数別サイズを実測(`0:09`=238px / `15:06`=247px / `1439:59`=246px)。
  375px幅・320px幅の両方に全ケースが収まる
- AI相談を押しても下のセット行の位置が動かないこと(524px→524px)を実測
- 記録0件・履歴0件の初期状態でクラッシュしないこと、4タブの巡回、日英表示を確認

## 残っている課題

- **`paddingBottom`のiOS実機での確認は未実施。** ブラウザでは`env()`が0のため、計算式が
  正しいことまでしか確認できていない。シミュレータでインターバルを展開して最下部まで
  スクロールし、保存ボタンが押せることを目視するのが確実
- 履歴パネルの1RM表記は「1RM 100.0kg」、履歴タブのバッジは「⚡1RM 100.0kg」で見た目が揃っていない
  (値は一致している)
- `hist:${idx}`が配列インデックス依存のため、コピー後6秒以内に別の日の記録を削除すると
  別カードに注記が出る。2.5秒の頃からある挙動だが、窓が2.4倍になった

---

# 2026-09-05(続き): カレンダーへの分割名表示と「現在の種目へ」ボタン(v105)

ユーザーからの要望2件。どちらも既存構造に無いもの(日付→分割名の対応、種目カードへの
アンカー)を足す必要があった。レビューで**v104と同じ型のsafe-areaバグを上側でも作っていた**
ことが分かり、そちらの修正が本題になった。

## 要望と対応

### 1. 履歴のカレンダー、赤丸の部分に分割の名前を表示

カレンダーのセルが持っていたのは `trainedDates`(日付キーのSet)だけで、`session` に到達
できなかった。`trainedSessions`(日付キー→分割名のMap)に置き換えた。

セルの `aspectRatio: "1"` を外して `minHeight: 46` にし、分割名を9pxで表示する。
セル幅は375px端末で41pxしかないので、長いカスタム名(「Chest & Triceps Day」等)は省略する。
**分割名が付いていない日(分割を設定せずに記録した日)は従来どおり赤い点**を出す。

### 2. 現在の種目に戻るボタン

「現在の種目」= まだRIRを入れていないセットを持つ最初の種目(ドメインの「RIRが入って初めて
実施済み」に沿わせた)。全部埋まっていれば最後の種目。種目カードに `id={ex-${ex.id}}` を
振ってスクロール先にした。

**ユーザーには「記録画面のどこかに常に表示」と確認していたが、常時表示にすると種目カードの
YouTubeボタンを覆って押せなくなるのを実機能確認で見つけたため、IntersectionObserverで
「現在の種目が画面に見えているときは出さない」ようにした。** 見えていれば戻る必要もないので
要望の意図は満たしていると判断したが、確認した仕様を実装側の都合で変えている点は記録しておく。

## reviewerの指摘で見つかった問題

### 重大: v104と同じsafe-areaの取りこぼしを、今度は画面の上側で作っていた

上部バーは `position: sticky` で自分で `env(safe-area-inset-top)` を負担している。つまり
実際の高さは **約95px + env(top)** で、ブラウザ(env=0)の95pxに対しiOS実機では142〜154pxになる。
当初 `scrollMarginTop: 110` と決め打っていたので、**iOS実機でだけ「押したのに種目名がバーの裏」**
になるところだった。v104のインターバルパネル(下側)とまったく同じ型。

`rootMargin` は `calc()`/`env()` を受け付けないため、そもそも式では書けない。
**バーとタブバーに `ref` を張って `getBoundingClientRect().height` を実測し、
スクロール位置(`scrollToCurrentEx`)とIntersectionObserverの両方をそこから作る**形に統一した。
`scrollIntoView` はscroll-marginでしか調整できず実測値を渡せないので、`window.scrollTo` に変えている。

検証は、バーの `paddingTop` とタブバーの `paddingBottom` にiPhone相当の値(59px / 34px)を
注入して行った。バー158pxの状態でもカード上端が8px下に着地することを確認済み。

### 改善提案として直したもの

- **固定ボタンが「ワークアウトを保存」の右3分の1を覆っていた**(実測で26px重なり)。保存ボタンは
  ルートの`paddingBottom`(90px)の上に高さ56pxで座るので下端から146pxを占める。ボタンを
  下端から100pxに上げて回避した。**「保存しようとしたら現在の種目へスクロールする」**という、
  全部埋めずに保存する動線で普通に起きる事故だった
- 固定ボタンだけ `right: 16` でビューポート基準になっており、520pxより広い画面でアプリ列の
  外に浮いていた。他の固定要素と同じく `maxWidth: 520` のラッパーに入れた
- **ラベルの「↑」を外した。** 現在の種目より上を見ているとき(前回実績を見に戻るのは主機能の
  動線)もボタンは出るので、矢印が逆を指すことがある
- カレンダーの分割名を赤字にしていたが、薄い赤背景の上でコントラストが3.3:1しかなく9pxでは
  読めない。トレーニング日であることは背景色が示しているので本文色にした
- 同じ日に2回記録し、2本目が分割なしだと名前が消えて点に戻る問題。名前が付いている方を残す
  ようにした。あわせて配列順への依存もやめた(SQLite経路は`workouts`に`position`列が無く、
  順序はSQL的に保証されていない)
- 分割名が空白のみのとき、名前も点も出ず薄い背景だけの日になっていた(`trim()`で判定)
- **履歴カードが分割名を`dayName()`に通しておらず、英語UIでカレンダーは「Chest」、真下の
  記録カードは「胸」と表記が割れていた**(既存バグ。今回隣り合って見えるようになったので直した)

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `index.html` | カレンダーの分割名表示、「現在の種目へ」ボタンとスクロール、バー/タブバーの実測(`topBarRef`/`navRef`)、履歴カードの`dayName()`適用 |
| `src/domain/i18n.js` | `log.toCurrent`を追加、`history.calendarHint`を実態に合わせて更新 |
| `sw.js` | `CACHE`をv104→v105 |

バージョン: v104 → v105。

## 検証内容

- `npm test` 271件通過(表示層のみの変更で`src/domain/`の純粋関数には及ばない)
- カレンダーを日英で実測。「Chest」26px /「Push」22px /「Upper」27px / 長いカスタム名
  (「Chest & Triceps Day」「背中と二頭」)は37pxで省略。いずれもセル幅41px内に収まる
- **iPhone相当のsafe-area(上59px / 下34px)を注入した状態で、スクロール位置が正しいことを確認**
  (バー158px、カード上端166px、隙間8px)
- 固定ボタンと保存ボタンが重ならないこと(保存ボタンの10px上)を実測
- 現在の種目が見えているときはボタンが出ず、離れると出ることを確認

## 残っている課題

- **iOS実機/シミュレータでの確認は未実施。** safe-areaは注入して再現したが、実機の
  `env()` が実際にその値を返すかまでは確認していない
- `rootMargin` は `calc()`/`env()` を受け付けないため実測値を入れているが、バーの高さが
  後から変わる場合(インターバルの開始/終了)は依存配列で張り直している。バー自体の高さが
  同一セッション中に変わる他の経路があれば追従しない
- カレンダーのセル幅41pxは4文字程度が限界で、長い分割名は何の日か判別できない

---

# 2026-09-05(続きの続き): 分割の日ごとのAI相談とインターバルの声かけ(v106)

## 要望と対応

### 1. タイマーのバーの下に声かけ

ゲージ下の「1分まで」「{n}分経過」を、経過に応じた3種の声かけに置き換えた。
経過分数は上の大きな数字が出しているので、この行は「今どうすべきか」だけを言う。

| 区間 | 文言 | 色 |
|---|---|---|
| 0〜1分 | しっかり休め | 緑 |
| 1〜2分 | そろそろ始めろ | 黄 |
| 2分〜 | パンプが冷めるぞ | 赤 |

**区切りはユーザーに確認して確定した。** 「1分のところ/2分のところ/3分のところ」という
指定は、経過時間そのもの(1分経過で1つ目)とも読めたが、**ゲージの3区画(1本目/2本目/3本目)
に対応させる**のが意図だった。ゲージのセグメント色ともこれで一致する。

### 2. 分割の各DayにAI相談ボタン

`dayToText(d, i)` を新設。種目単位(`exerciseToText`)が「次の重量・回数」を聞くのに対し、
こちらは「この日の種目の選び方・並び・量」を聞く。

**他の日の担当部位も渡している。** これが無いと、AIは「胸の日に背中も入れては」のような
分割の設計と衝突する助言をしてくる。

ボタンはヘッダー行(▲▼・今日はこれ・編集で埋まっている)ではなく部位チップ行の下に置いた。
ペースト注記はボタンの左に横並びにしてある(下に出すとDayの数だけ高さを常時確保することになる)。
**カードは丸ごとタップで記録画面へ飛ぶ領域なので`stopPropagation`が要る。**

## reviewerの指摘で見つかった問題

### 重大: 自重種目のプロンプトが「0kg(体重込)」になっていた

記録側の`weight`は加重(プレート分)だけなので、懸垂を加重なしで記録した日は
`0kg(体重込)×10` と出ていた。**AIは「体重込みで総負荷0kg」と読む。** しかも
`dayToText`は体重を渡していないので、復元する手がかりも無かった。自重メインの日ほど
助言が壊れる。

`exerciseToText`が同じ`share.withBw`を使っていて成立していたのは、(a)セット行ではなく
メタ行に置き、(b)体重を別に渡し、(c)基準の違いを注記していたから。`dayToText`はその3つを
持たずにラベルだけコピーしていた。

`dayToText`では種目ごとに単位が変わるためメタ行に逃がせないので、**数値の側を実効重量
(体重×係数+加重)に置き換えた**。懸垂が `75kg(体重込)×10`(体重75+加重0)、
`85kg(体重込)×5`(体重75+加重10)と出るようになり、ラベルと数値の意味が一致した。

### 改善提案として直したもの

- **セット表記のコピーが3本目になっていた**(`workoutToText`のインライン / `exerciseToText`の
  `setLine` / 今回の`dayToText`)。v104で「同じ計算が3通りに割れていた」のを直したばかりなので、
  `shareSetLine(s, weight, unit)`に集約した。重量と単位は呼び出し側で作って渡す
  (記録中は表示単位の文字列、保存済みはkg、自重は実効重量、と作り方が違うため)
- **Dayの並べ替えで別のカードに「✓ コピー」が出る経路**。`editDay`は`moveDay`で付け替えて
  いるのに`copiedIdx`はしていなかった。同じ処理を足した
- 英語UIのプロンプトに和文の中黒(`・`)が漏れていた
- 種目メモ(`exerciseNotes`)を入れた。「右肩に違和感」のようなメモは、種目の選び方を聞く
  この用途でこそ効く
- 英語の声かけの文末ピリオドを外した(他のUI文言に揃えた)
- **インターバルパネルの高さを再測定した**(コメントが「中身を変えたら実測し直すこと」と
  要求していた)。243pxで、コメントにあった266pxとずれていた。時計の桁数でフォントサイズが
  変わるため高さも変わることを含めてコメントを更新した

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `index.html` | `shareSetLine`の集約、`dayToText`の新設、分割タブのAI相談ボタン、声かけ行、`moveDay`での`copiedIdx`付け替え |
| `src/domain/i18n.js` | `rest.coach1/2/3`(旧`rest.until1min`/`rest.elapsed`は参照が消えたので削除)、`share.day*`を9キー追加 |
| `sw.js` | `CACHE`をv105→v106 |

バージョン: v105 → v106。

## 検証内容

- `npm test` 271件通過
- 声かけを実機能確認(0:11「しっかり休め」緑 / 1:41「そろそろ始めろ」黄)
- **生成プロンプトをクリップボード経由で日英とも実測。** 自重種目が実効重量になること、
  ダンベルが「(片手)」、ウォームアップが`[W]`、他の日の担当・種目メモが入ることを確認
- 分割タブでボタンを押しても記録画面に飛ばないこと(`stopPropagation`)を確認
- インターバルパネルの高さを再測定(243px)

## 残っている課題

- `dayToText`は`#appsrc`内にあるためテストで縛れない。今回の「単位の意味づけの誤り」は
  `src/domain/`に出していれば1行のテストで捕まる種類だった
- Day名をリネームすると`w.session`との紐が切れて履歴が空になる(`lastSameSession`も同じ
  前提で動いているアプリ全体の割り切り)
- ペースト注記は320px幅では「※AIの画面に貼り付けて送…」と切れる
- パネル内で赤の意味が2つある(枠線と数字は180秒、ゲージのセグメントと声かけは120秒)。
  既存の不整合で、今回は声かけをゲージ側に合わせた

---

# 2026-09-05(4回目): 「推移」が実態を反映していなかった(v107)

ユーザーが実機のスクリーンショットを見て「下部の推移10→15→15→15の表示が実態を反映してるか確認」
と質問したのが発端。**表示は仕様どおりだったが、仕様自体が実態を映していなかった。**

## 何が起きていたか

`exerciseInsight` の `topOf` が「ウォームアップ・補助を除いた中で最も重いセット1つ」を返し、
その**重量だけ**を並べていた。実機の記録(懸垂・自重種目)で照合すると:

| 日付 | 実際のセット | 推移の値 |
|---|---|---|
| 7/20 | 10×8, 0×12 | 10 |
| 7/25 | **15×13**, 0×12 | 15 |
| 7/29 | 15×8, **15×9**, 15×8, 0×15, 0×15 | 15 |
| 8/19 | 0×16, 0×17, **15×9**, 0×7, 0×14, 0×12, 0×12, 0×11 | 15 |

消えていた情報が3つある:

- **回数**: 7/25は15kgで13回、8/19は9回。落ちているのに「15 → 15」で横ばいに見える
- **セット数**: 7/29は15kgを3セット、8/19は1セット
- **自重のみのセット**: 8/19は8セット中7セットが自重(実効78kg)。ボリュームは7,191kgで
  7/29の4,335kgを上回るのに、推移には一切現れない

そのため **15kg×13回→9回と後退している局面で「⚠ 重量が3回連続で頭打ち。そろそろ上げどき」**
が出ていた。重量を上げる前に回数を戻すべき状態で、逆方向を促していたことになる。

## 対応(ユーザーが選択)

- 推移に回数も出す: `10×8 → 15×13 → 15×9 → 15×9`
- アラートの判定に回数を含める: 回数が直近の最高を下回っている間は「上げどき」と言わず、
  `log.repsDown`(「この重量での最高 13回 / 前回 9回」)で事実だけ渡す

## reviewerの指摘で追加で直したもの

1回目のレビューで重大は無かったが、実データに直結する指摘が出た。

- **同じ重量なら回数が多いセットを代表にする。** `reduce` が `>` だったため先頭優先で、
  7/29の `15×8 / 15×9 / 15×8` が **`15×8`** として推移に出ていた。達成した15×9が消えていた
  (今回の主題そのもの)
- **比較範囲を直近3セッションに限定。** ストリーク全期間の最大と比べていたため、
  ずっと前の突出した1回が「上げどき」を無期限にブロックする。実データの13回は
  その重量を1セットしかやらなかった日の値で、まさにこれに当たる
- **加重できない種目でアラートを出さない**(`canAddWeight`)。腕立てのように常に加重0の種目は
  `streak` が無限に伸び、「重量が頭打ち。上げどき」と出ていた。取れる行動が無い助言だった
- **全比較を数値化。** `weight` は保存時に数値化されるが、旧データやバックアップ復元経由で
  文字列が残り得る。`"9" > "15"` が辞書順で true になりトップセットを取り違える
- 文言から矢印と連続回数の重複を削除(`{best}`は「前々回」ではなく直近数回での最高)

## `src/domain/insight.js` へ移設した

判定の分岐が「ストリーク境界」「回数の同値」「トップセット不在」「加重0」と増え、
しかもどれも**実データでしか露見しない**種類だった(今回2件を実際に踏んだ)。
`#appsrc` の中に置いておく粒度を超えたので切り出した。`resolveIsDb` を注入する形は
`coefficients.js` と同じパターン。

## 変更したファイル

| ファイル | 内容 |
|---|---|
| `src/domain/insight.js`(新規) | `exerciseInsight` / `topSetOf`。判定の集約先 |
| `index.html` | `exerciseInsight` を移設先の呼び出しに置換、アラートの表示条件、推移の表示形式、LIBSに追加 |
| `src/domain/i18n.js` | `log.repsDown` を追加 |
| `tests/insight.test.js`(新規) | 44件。実データで踏んだ2件を含む |
| `sw.js` / `scripts/sync-www.js` | v107、新規ドメインファイルの参照元3箇所 |

バージョン: v106 → v107。

## 検証内容

- `npm test` 315件(新規44件)
- **実機の記録を再現して両分岐を実測**: 前回9回では `log.repsDown`、14回に変えると
  `log.plateauAlert` が復活。推移が `15×8` → `15×9` に直ることも確認
- 8765(ランタイムBabel)・8766(事前ビルド)の両方で新規ファイルが200で読まれ、
  `window.exerciseInsight` が関数として存在することを確認

## 2回目のレビューで追加で直したもの

移設と修正を入れた差分に対してもう一度レビューを通したところ、**今回の変更で新しく出るように
なった表示**が見つかった。

- **加重できない種目(腕立てなど)の推移が `0×20 → 0×22 → 0×25` になっていた。**
  v106までは `0 → 0 → 0` だったので、回数を足したことで初めて目に見える形になった。
  `canAddWeight` を推移と連続表示にも適用し、加重0の種目では回数だけ(`20 → 22 → 25`)を出す
- **`log.repsDown` の「この重量での最高」が、実際は直近3セッションの最高だった。**
  推移は4件出るので、4件目に最高記録があると同じ行の中で矛盾する
  (推移「15×13 → …」の隣に「最高 10回」)。文言に範囲を書いて実装と一致させた
- **「回」がセッション数とレップ数の両方を指して隣り合っていた。**
  「15kg で 3回連続 / 最高 13回 / 前回 9回」の左端だけがセッション数。
  `log.streak` / `log.plateauAlert` / `share.exStreak` を「セッション」に統一した

## 残っている課題

- (v108で修正済み) 補助ありセットだけの日で前回実績が消える件。下記参照
- `topSetOf` が加重ありセットを代表にする構造は変えていないので、自重のみのセットは
  依然として推移に現れない。実効重量で見る案は、全セットが体重以上になり加重の差が
  見えにくくなるトレードオフがある
- `streak === 1` のとき `readyToProgress` は常に true になる(比較対象が前回自身のみ)。
  UIは `streak >= 3` でしか出さないので実害はない

---

# 2026-09-05(5回目): 補助ありセットだけの日で前回実績が消えるバグ(v108)

v107のレビュー中に見つかった既存バグ。ユーザーの指示で対応した。

## 症状

その日の全セットに「補助あり」を付けて保存すると、**その種目の「前回 8/24」「推移」
「セット行のゴースト表示」が丸ごと消える。** しかも1つ前の有効なセッションへの
フォールバックが無いので、**間に1日挟むだけでそれ以前の記録も見えなくなる。**

アシストマシンを常用する種目(アシスト懸垂など)では、
「前回の実績をそのまま見せて、判断はユーザーに委ねる」という中心機能が働かない。

## 原因

`exerciseInsight` が「代表セットが取れない」と「前回のセットを見せる」を同じ`null`で
束ねていた:

```js
const prevTop = topSetOf(prev.ex);
if (!prevTop) return null;   // ← ゴースト表示の入力(sets)ごと失われる
```

`topSetOf` は warmup・assisted・回数0を除くが、**assisted は保存時のフィルタ
(`ex.sets.some(s => !s.warmup)`)を通過する**ので、通常操作で到達する。
全セットがウォームアップの場合は保存時に潰されるため到達しない(そちらは無症状だった)。

## 対応

日付・前回のセット・推移は、代表セットの有無と無関係に返すようにした。重量にまつわる
判断材料(連続・上げどき)だけを`topWeight: null`で畳む。推移は代表セットを持たない日を
飛ばすので、直近が補助ありでもそれ以前の推移が出る。

あわせて`canAddWeight`の判定を「前回1回」から「推移の範囲に一度でも加重があるか」に変えた。
前回だけを見ると「今回はたまたま自重だけだった懸垂」を加重不可と誤判定し、推移が
回数だけの表示に化ける。

## 検証内容

- `npm test` 321件(insight.jsのテストを50件に拡張)
- **修正を元に戻すと5件落ちることを確認**(テストが実際にこのバグを捕まえる)
- 実データで確認: 前回が全セット補助ありの状態で「前回 8/24 (月)」「推移 10×8 → 10×9」
  「ゴースト表示 補:0×12 RIR1 / 補:0×10 RIR0」が出て、重量の判断材料だけが畳まれる

バージョン: v107 → v108。
